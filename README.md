# PROJECT_NAME (automotive AI media platform)

`PROJECT_NAME` / `DOMAIN.COM` are placeholders — see `.env.example`. Nothing
in `apps/`/`packages/` hardcodes a brand or domain name.

Start with `docs/architecture.md` — it links to the rest of `docs/` and has
the full status table below in context.

## Status

| Area | Status |
|---|---|
| Design docs (architecture, DB, AI pipeline, editorial system, security, deployment) | **Done** — `docs/` |
| Database schema (50 tables) | **Done, migrated & verified against a real PostgreSQL 17 instance** — see `docs/database.md` for the full ERD and non-obvious decisions. **Real, if minor, doc-accuracy gap found and fixed 2026-09-08**: this row and `docs/database.md`'s own opening line both said "51 tables" — counted directly (`grep -c "^model "` against the real schema) rather than continuing to repeat a figure neither had verified recently: it's 50. Also found and corrected `docs/database.md`'s "Migrations" section, which still claimed the original `..._init` migration was "the current single migration" — 3 more real, dated migrations have landed since (`pg_trgm` extension + trigram index, a real `Source.url` uniqueness constraint, and a real fix dropping `EntityRelation.fromId`'s three simultaneously-impossible FK constraints, verified by reading each migration's own SQL, not just its folder name). **Real gap found and fixed 2026-09-08 (same day, one more pass), plus a real self-inflicted mistake caught and corrected in the same tick**: `Story`/`SearchQuery`/`SystemAlert` all order real paginated queries by a timestamp column with no index backing that sort — unlike `AuditLog`, already correctly indexed on its own `createdAt`. `SearchQuery` was the most urgent instance (722+ real accumulated rows already, not hypothetical). Added the 3 missing indexes and ran a real `prisma migrate dev` to generate the migration — which auto-generated an UNINTENDED `DROP INDEX` on the real, load-bearing `source_articles_title_trgm_idx` GIN index (created via raw SQL in an earlier migration, so it has no representation in schema.prisma's own `@@index` syntax — Prisma's diff concluded it shouldn't exist). Verified live immediately after applying that this genuinely dropped the real index from the real database, not just in the generated SQL — a real, self-caused regression to the duplicate-detection/clustering pipeline this whole project depends on. Restored it live, then handled the fix the historically-correct way per Prisma's own model (an already-applied migration's checksum must match its recorded content — tried editing the applied migration directly first, recognized that was wrong, reverted): added a SEPARATE new migration that re-creates the index instead, leaving the original (flawed) migration's history intact. Verified the final state directly against `pg_indexes` (all 4 target indexes present) and ran a real live `ingest:once` against all 3 real feeds end-to-end (0 errors) to confirm the restored index actually works, not just that it exists. **Closed out the same day, one more pass**: rather than leave that landmine flagged-but-unfixed, checked directly (not assumed) whether this Prisma version (`5.22.0`) actually needs a preview feature for extended index types — it doesn't, verified live via `prisma validate` and `prisma migrate diff`. Declared the real index directly in schema.prisma (`@@index([title(ops: raw("gin_trgm_ops"))], type: Gin, map: "source_articles_title_trgm_idx")`, the explicit `map:` preserving its exact existing name) — this produced a genuinely EMPTY migration diff (schema now accurately describes reality, zero database change needed), permanently closing the landmine: no future `prisma migrate dev` run can drop this index again. See `docs/database.md`'s new "non-obvious decisions" #6/#7 for the full story, including why the ALREADY-APPLIED migration's own comment (which said this couldn't be done cleanly) was correctly left un-edited rather than corrected — editing an applied migration's content breaks its checksum, the exact lesson learned earlier in this same tick. Re-verified with a second real live `ingest:once` run (0 errors) after this change. **Real documentation gap found and fixed 2026-09-07**: while checking every quoted `docs/*.md "Section"` cross-reference in the codebase for accuracy (found by fixing an unrelated wrong-file pointer on `Source.trustScore`, which pointed at `docs/ai-pipeline.md "Source reliability"` when that section actually lives in `docs/editorial-system.md` — corrected), found two more comments (`SourceArticle.excerpt`, `apps/worker/src/backfill-authors.ts`) already quoting `docs/security.md "no full-text mirroring"` as if it existed — it didn't; the section had never actually been written. Added it for real (`docs/security.md` "No full-text mirroring": this project never mirrors a source's full article text, only short excerpts/quotes with real attribution) and updated `FactEvidence.quote`'s own comment (previously pointed at the file with no section name at all) to name it explicitly too — all three references now resolve to real content. Docs/comments-only change (`packages/database/prisma/schema.prisma` comments + `docs/security.md`), no code executed differently; `prisma validate` confirmed the schema itself still parses. **Real, standalone finding, 2026-09-08 (separate pass, same day)**: reviewing every schema model fresh for internal correctness, found `Category` and `Tag` are unlike every other not-yet-wired-up model checked this session (`AIJob`, `Image`, `Comment`, etc. -- each missing only a real write path, but still tied into the graph via real relation fields and a real doc section describing intended use): these two connect to NOTHING -- zero application code references either one, zero mentions in any `docs/*.md` file (including this doc's own ERD, which otherwise covers every real entity group), and no other model declares a relation pointing at either. `Topic` (real relation, real code, real tests) already covers the classification role these may once have been meant for. Left both in place rather than deleted -- genuine uncertainty about intent (an early draft superseded by `Topic`? forward provisioning for a never-written-down future need?) is a reason to document clearly, not delete without being sure -- documented directly on both models in `schema.prisma` and as a new `docs/database.md` "non-obvious decision" #8. Verified genuinely comment-only via the same discipline as the fix above: `prisma validate` passed, `prisma migrate diff` against the real dev database produced a literally empty migration, and the full regression sweep (`npm run typecheck` all 10 workspaces, `npm test` 282/282) stayed clean after regenerating the Prisma client. **Real, fixable gap found and fixed 2026-09-08 (separate pass, same day, right after the Category/Tag finding above)**: continuing the same fresh schema review, found `Citation.factId`/`sourceArticleId` had no `@relation` at all -- bare, unlinked `String?` columns, unlike `Citation.articleId` right above them, which correctly did. Unlike `EntityRelation`/`AIAgentAction`'s deliberately polymorphic edges, `factId`/`sourceArticleId` each point at exactly one concrete, non-polymorphic model -- no structural reason for these two to differ from `articleId`. Unlike the Category/Tag finding, this one had a clear, low-risk fix rather than genuine ambiguity, so fixed it directly rather than just documenting it: added the missing `Citation.fact Fact?`/`Citation.sourceArticle SourceArticle?` relations (both still correctly optional) plus the matching reciprocal `citations Citation[]` fields on both `Fact` and `SourceArticle`. Verified via `prisma migrate diff` before applying anything -- a real, minimal two-line diff (exactly the two new `ADD CONSTRAINT` statements, no unintended drops), the same discipline this project's own trigram-index incident established. Applied via a real `prisma migrate dev` (migration `20260908051629_add_citation_fact_source_article_relations`), safe given zero real `Citation` rows exist yet to violate the new FKs. Confirmed live against real Postgres constraint metadata (`pg_constraint`) that all three real FKs now exist exactly as declared, not just trusting Prisma's own tooling. Full regression sweep clean: typecheck (all 10 workspaces), 282/282 unit/integration, 66/66 E2E. Also found and fixed the same tick: `docs/database.md`'s own "Migrations" section had already gone stale again within the same day (missing 3 real migrations landed since its own prior correction) -- rather than re-enumerate by hand a third time, changed the maintenance approach entirely to point at the real migrations directory as the source of truth, matching the precedent already set for `docs/architecture.md`'s API-endpoint-list section.  **Second, related fix, same tick, but live not dormant**: continuing the same review, found the identical gap shape on `SourceAuthor.sourceId` -- but unlike `Citation` (0% built, zero real rows), `SourceAuthor` is real and currently in use: `apps/worker/src/ingest.ts`'s `resolveSourceAuthor()` already creates real rows during real RSS ingestion (9 real rows in this dev DB). Before adding a real FK constraint, queried every existing row live and confirmed all 9 have a genuine, valid `sourceId` (zero null, zero dangling) -- only then safe to apply. Added `SourceAuthor.source Source?` and the reciprocal `Source.authors SourceAuthor[]`, verified via `prisma migrate diff` (a real, minimal one-line diff) before applying, migration `20260908052845_add_source_author_source_relation`, confirmed live against real `pg_constraint` metadata, full regression sweep clean (typecheck, 282/282, 66/66 E2E -- the e2e run specifically because this table has real, currently-flowing data unlike the Citation fix above). See `docs/database.md`'s new "non-obvious decisions" #9 and #10 for the full story on both. **Third instance, same tick, found by switching methodology**: after hitting the identical gap shape twice by manual spot-checking (Citation, SourceAuthor above), switched to a systematic sweep -- a small script scanning every `*Id String` field in the whole schema for whether the very next line declares a `@relation`. Most hits were false positives (junction-table composite keys where the relation appears two lines down; `AuditLog.actorUserId`, whose relation is validly declared just before its own scalar field -- Prisma allows either order; the already-confirmed deliberate polymorphism on `EntityRelation`/`AIAgentAction`). One real hit: `Article.authorUserId` -- bare, unlinked, while `ArticleRevision.authorUserId` (identical concept, same file) already does this correctly. `Article` has zero real rows (dormant like Citation, not live like SourceAuthor), so no live-data safety check was needed. Added `Article.author User?` and the reciprocal `User.articles Article[]`, verified via `prisma migrate diff` (a real, minimal one-line diff), applied via `prisma migrate dev` (migration `20260908053909_add_article_author_user_relation`), confirmed live against real `pg_constraint` metadata. Ran the full regression sweep including E2E despite `Article` itself being dormant, since the change alters `User`'s own generated Prisma type and `User` is central to every real auth/admin flow: typecheck (all 10 workspaces), 282/282, 66/66 E2E, all clean. **Side-finding checked immediately given the stakes**: `User.status` defaults to `UserStatus @default(INVITED)` in the schema, not `ACTIVE` -- read the real `POST /v1/users` handler directly and confirmed it explicitly sets `status: "ACTIVE"` in its `prisma.user.create()` call, so the schema default is never actually reached by real code today. Not a live bug, but a real footgun for any future code path (bulk import, a second creation endpoint) that creates a `User` without remembering to override it -- documented both as a new `docs/database.md` "non-obvious decision" #11 and as an inline comment directly on `User.status` in `schema.prisma` itself, so it's visible even to someone reading the schema in isolation. **Self-inflicted irony found and fixed 2026-09-08 (next pass)**: `docs/database.md`'s own "Migrations" section, having already rotted twice hand-enumerating migrations by name, had committed to "point at the real directory, don't re-maintain a copy" -- but then hand-typed a specific count ("7 real migrations total") right next to that commitment. 2 more real migrations (the SourceAuthor and Article relation fixes above) landed the same day and made that count stale too, same failure mode one level down. Fixed by removing the hardcoded count entirely -- the section now points at `ls packages/database/prisma/migrations/` for the live count instead of asserting a number that can only ever go stale again. Verified the real directory currently holds 9 migrations (confirmed via `ls`, cross-checked against every migration named in this session's own notes -- no drift). Docs-only change, no schema/code touched. **Broader sweep, real gap found and fixed 2026-09-08 (next pass, moved beyond the schema file itself)**: checked every `packages/*` directory directly rather than continuing to only audit `schema.prisma` -- `packages/search/` exists but is completely empty (no `package.json`, no source files, confirmed via `ls -la`/`find`), yet `docs/database.md`'s pgvector-migration note told a future implementer to "update `packages/search`" for real vector-search query logic. Grepped the whole `apps/` tree for `cosine`/`similarity`/`embedding` and confirmed the real clustering/dedup logic lives entirely in `apps/worker/src/ingest.ts`'s `findClusterCandidate()` (a `pg_trgm` `similarity()` raw SQL query, not an "application-side cosine similarity loop" as the old note also mischaracterized it) -- `packages/search` has never held any real code. Fixed the doc to name the real file instead of a package with nothing in it; left the empty directory itself in place given genuine uncertainty about intent (Phase 2 forward-provisioning vs. stray leftover), same posture as the Category/Tag finding above. Docs-only change. |
| RSS ingestion → duplicate detection → Story clustering | **Done, tested end-to-end against real feeds (Electrek/InsideEVs/Motor1) and a real database**. **Real gap found and fixed 2026-09-07**: `SourceAuthor` sat entirely unused since the initial schema scaffold — every real byline the crawler ever saw was silently discarded. Verified live against all 3 real seeded feeds before building: electrek.co's real feed (WordPress) carries a real `<dc:creator>` per item; insideevs.com and motor1.com (both `Laminas_Feed_Writer` output) carry no author field at all — an honest per-source gap, not papered over with a fake fallback. Fixed with `apps/worker/src/rss.ts`'s `parseFeed()` extracting `dc:creator` (RSS 2.0), falling back to the plain `<author>` element's "email (Name)" convention, and Atom's `<author><name>`; `ingestSource()` resolves/links a real `SourceAuthor` row scoped per source. Verified against the real, live electrek.co feed (not just a fixture): all 100 current real items correctly yielded a real author name. Now shown on `/admin/stories` (each story's real byline(s), where available) via `GET /v1/stories` including each story's real `SourceArticle.author`. A new `apps/worker/src/backfill-authors.ts` (`npm run backfill:authors`, idempotent — same shape as `backfill-topics.ts`) retroactively links pre-existing authorless `SourceArticle` rows for items still present in each source's *current* feed (a real, honest limit, not a full recovery: this project never mirrors full feed content, so an article that has scrolled out of its source's feed window is gone for good — same real run: 100 pre-existing Electrek articles backfilled with their real byline, 0 for InsideEVs/Motor1 since neither feed carries author data at all). **Real gap found and fixed 2026-09-07 (robots.txt compliance)**: `Source.robotsStatus` ("cached robots.txt evaluation") also sat unused since the schema scaffold — this crawler fetched every feed on every cycle without ever checking whether the source's robots.txt actually permits it. Fixed with `apps/worker/src/robots.ts`'s `isPathAllowed()` (a minimal but real robots.txt parser: `User-agent` grouping including this bot's own named group taking precedence over `*`, and the standard longest-matching-rule-wins semantics so a narrower `Allow` can override a broader `Disallow`) and `checkRobotsAllowed()`, called by `ingestSource()` before every feed fetch — a genuine `Disallow` skips the fetch entirely, records a real `WARNING` `SystemAlert` (deduped the same way crawl-failure alerts already are), and caches the result on `Source.robotsStatus`, now shown on `/admin` whenever it's the actionable "disallowed" state. Verified live against all 3 real seeded sources' actual robots.txt files (fetched directly) before writing any code, and again after wiring it in via a real `ingest:once` run: none currently disallow their feed path, so this closes a real compliance gap that happens to be a no-op today (a source could add a feed-path restriction in the future, or a newly-added source might have one from day one) — not a hypothetical fix, a real check that currently passes. **Real gap found and fixed 2026-09-07 (same day, later pass, wildcard robots.txt patterns)**: `isPathAllowed()`'s path matching was a plain `path.startsWith(rule.path)`, treating `*`/`$` as literal characters instead of the real robots.txt extended syntax (RFC 9309 §2.2.3: `*` matches any character sequence, a trailing `$` anchors to the end of the path) — not hypothetical, verified against electrek.co's own real robots.txt (fetched live again to confirm), which already has `Disallow: /*?s=`/`/*&s=`/`/*ep_filter_` in active use. Those particular rules happen not to match `/feed/` (the path actually crawled), so the plain-prefix version reached the right answer today by coincidence, not real understanding — the first future Disallow rule whose wildcard *did* cover a crawled path would have been silently ignored, a genuine compliance violation. Fixed with a small pattern-to-regex compiler (escapes everything except `*`→`.*`, honors a trailing `$` anchor); a plain literal path still matches exactly as before, so no regression for the common wildcard-free case. 3 new tests, including one using electrek.co's own real `/*?s=` rule as the fixture. **Real gap found and fixed 2026-09-07 (same day, one more pass, malformed feedUrl)**: `checkRobotsAllowed()`'s `new URL(feedUrl)` call sat outside its own try block — a malformed `feedUrl` threw synchronously, which an async function turns into a REJECTED promise rather than a caught one, directly contradicting this function's own documented "never blocks on network trouble" fail-open contract. Verified live before fixing (`node -e`): confirmed the rejection propagates uncaught. Compounding gap: neither `POST /v1/sources`'s `url` nor `feedUrl` field validated it was an actual well-formed URL at all — a malformed one reaching the crawler would waste real BullMQ retry attempts on a URL that can never succeed, instead of failing fast where a human enters this data. Fixed both: moved the `new URL()` call inside the try block (now correctly fails open, same as any other robots.txt-reachability trouble), and added `.url()` to both schema fields. 1 new `apps/worker` test (fails open for a malformed feedUrl, confirms `fetch` is never even called) + 1 new `apps/api` test (rejects a malformed `url`/`feedUrl` on create). **Real reliability gap found and fixed 2026-09-07 (fetch timeouts)**: neither the feed fetch (`ingest.ts`) nor the robots.txt fetch (`robots.ts`) had any timeout at all — a real hanging source server (under load, a network issue, not contrived) would block that job indefinitely; with BullMQ's concurrency of 5 (see scheduler row below), enough simultaneously-hung sources could eventually starve every real ingestion slot. Also directly contradicted `checkRobotsAllowed()`'s own documented promise to "never block on network trouble reaching robots.txt" — a hang is network trouble, it just wasn't failing fast. Fixed with `AbortSignal.timeout()` on both (30s feed / 10s robots.txt, both well inside the shortest real `crawlInterval`, 900s) — a timeout throws the same shape of error the existing try/catch already handled, so no new error-handling path was needed. Confirmed the mechanism genuinely works in this Node runtime with a real request to a non-responsive test address (aborted in ~2s for a 2s timeout, not indefinitely) before trusting it in the fix. 3 new tests (2 proving the real `fetch` call now carries a genuine `AbortSignal`, 1 proving a timeout-shaped rejection is handled exactly like any other network failure) + a real live `ingest:once` run against all 3 actual feeds confirming zero regression (0 errors, real stories still created). **Real test-coverage gap found and fixed 2026-09-08**: `rss.ts`'s own internal `parseDate()` already guards against a malformed `pubDate` with a real `Number.isNaN(d.getTime())` check (`new Date("garbage")` doesn't throw, it silently produces an "Invalid Date" whose `.getTime()` is `NaN`) — real, external, untrusted feed data, exactly the kind this file's author-extraction fixes above were built around — but nothing had ever exercised that guard; every existing test used a real, well-formed `pubDate`. If this guard were ever accidentally weakened or removed, nothing would have caught it. Added 2 new tests (a real malformed `pubDate` string, and an item with no `pubDate` element at all) confirming `publishedAt` comes back `null`, not a silently-invalid `Date` or a thrown error. **Real gap found and fixed 2026-09-08 (same day, one more pass, URL normalization)**: `apps/worker/src/url.ts`'s `normalizeUrl()` — the first, cheapest duplicate-detection signal this whole ingestion pipeline relies on — stripped any query param key matching "ref" as a bare PREFIX, not just the real referral-tracking convention (the exact key `ref`, or Twitter/Facebook's own underscore-delimited `ref_src`/`ref_url`). This also matched real, unrelated params that merely start with the same letters — `reference`, `refund`, `referrer` — none of which are tracking noise; silently stripping one would risk two actually-different articles normalizing to the identical URL and hashing as duplicates, a false-duplicate against the very system this function exists to protect. Fixed by requiring `ref_` (matching `utm_`'s own existing correct underscore-delimited pattern) or an exact `ref` match, rather than a bare prefix. 3 new tests: the real tracking convention (`ref`, `ref_src`) still correctly stripped; three real, plausible unrelated params (`reference`, `refund`, `referrer`) now correctly preserved. **Real test-coverage gap found and fixed 2026-09-08 (same day, one more pass)**: `ingestSource()`'s own primary crawler-failure path (a source's server returning a real error, or a genuine network failure — more common in practice than the already-well-tested robots.txt-disallow path) had never been directly tested at all — neither the real `WARNING` `SystemAlert` it creates on a genuine feed-fetch failure, nor its own "only alert on a NEW failure" dedup logic (`source.lastError !== message`), a real, non-trivial piece of business logic preventing alert-spam from a source stuck failing every crawl cycle. Added 2 new tests: a real feed-fetch failure (mocked `HTTP 500`) creates the real alert and updates `Source.lastError`/`lastErrorAt`; the identical failure repeating on the next cycle does NOT re-alert, but a genuinely different failure (`HTTP 404`) DOES alert again — proving the dedup keys on "same message as last time", not "never alert twice for this source." Both passed on the first run — the existing logic was already correct, this closes a real, previously-uncaught regression risk on an operationally important observability path. |
| `apps/worker`'s scheduler — now real BullMQ, reacting to admin changes at runtime | **Done, unit + live-verified** — two real increments landed together. First (2026-09-06): the old `setInterval` scheduler built its timer map once at startup and never looked again, so the admin dashboard's real "Sources" management (`PATCH /v1/sources/:id`) silently had no effect on a running worker until manual restart — fixed with `ingestSource()` checking `active` immediately, plus a polling reconcile loop. Second, bigger one (2026-09-07): **migrated to real BullMQ**, unblocking the Redis-version limitation noted below — a real modern Redis (8.10.1, portable Windows build) was downloaded with explicit user go-ahead (see docs/database.md "Local dev Redis"), replacing the incompatible pre-existing v3.0.504. `apps/worker/src/scheduler.ts`'s `reconcile()` now uses BullMQ's real Job Scheduler API (`upsertJobScheduler`/`removeJobScheduler`/`getJobSchedulers`) instead of a hand-rolled timer Map — real retries, persistence, and a real job store, none of which the `setInterval` version had. **A real bug was caught mid-build, not assumed away**: the actual objects `getJobSchedulers()` returns key the scheduler by `.key`, not `.id` (which is `undefined` on every real object despite being in BullMQ's own TS type) — using `.id` meant the removal branch silently never ran for any scheduler; caught by inspecting BullMQ's actual Redis-backed state directly, not by trusting the type or the absence of an error. Also caught and removed a redundant manual "run immediately" `queue.add()` call that was double-ingesting every source on startup — `upsertJobScheduler` already runs the first job immediately on its own. 3 new integration tests (`apps/worker/src/__tests__/scheduler.test.ts`) against the real local Redis, plus full live verification against the real running worker: startup scheduling (no duplication), real deactivation → scheduler removal, real `crawlInterval` change (900→1200s) → reschedule, all confirmed via direct BullMQ/Redis state inspection, not just log-watching. CI gained a real `redis:7-alpine` service container to match. **Self-caught follow-up, same day**: the migration's own claim of "real retries, concurrency control" was checked against the actual code and found false as shipped — neither `attempts`/`backoff` nor `concurrency` were ever configured (BullMQ defaults to `attempts: 1`/no retry and `concurrency: 1`). Fixed with real config (`attempts: 3` + exponential backoff on the job template; `concurrency: 5` on the Worker) and verified each independently with throwaway scripts against the real Redis: a job that always throws genuinely retries exactly 3 times with growing delays before failing permanently; 5 jobs added at once genuinely run concurrently (`maxConcurrentSeen: 5`, not serialized). **Stale-comment gap found and fixed 2026-09-08**: `apps/worker/src/run-ingest-once.ts`'s own comment still described this file as "currently a plain per-source setInterval loop... not yet BullMQ-backed" — never updated when the migration above actually happened. Comment-only fix, found by sweeping the codebase for other references to the pre-migration architecture. **Real, severe gap found and fixed 2026-09-08 — arguably this session's most severe finding after the `apps/api` `marketId` crash bug, same shape, in the process that's supposed to run unattended forever**: `apps/worker/src/index.ts`'s periodic `reconcile()` call ran on a bare `setInterval(() => reconcile(queue), 60_000)` with no error handling at all. Verified live (a real subprocess, this exact Node version) before assuming anything: an unhandled rejection thrown inside a `setInterval` callback is fatal on modern Node — it kills the entire process immediately, not silently swallowed the way older Node versions behaved. `reconcile()` does real DB (`prisma.source.findMany`) and Redis (`getJobSchedulers`/`upsertJobScheduler`/`removeJobScheduler`) calls, any of which can genuinely fail transiently — a real, plausible operational hiccup. The one-off startup call to `reconcile()` was already safely covered by `main().catch()`, but every subsequent call — every 60s, for the entire lifetime of this always-running worker — had zero protection: a single transient DB/Redis hiccup during ANY periodic reconcile would crash the whole worker process, taking the actively-running BullMQ `Worker` down with it (halting all real scheduled ingestion, not just the reconcile step) until someone noticed and manually restarted it. Fixed by wrapping the periodic call in a named `scheduleReconcile()` that `.catch()`es and logs any failure (structured JSON, matching this codebase's existing error-log style) instead of letting it propagate. A reconcile failure alone doesn't halt ingestion itself (already-scheduled BullMQ Job Schedulers keep firing independently), so this doesn't need a new `SystemAlert` channel — just needed to stop being able to crash the process. Verified live: a real throwaway subprocess reproducing the exact fixed pattern survived 4 consecutive simulated reconcile failures without dying (each correctly logged); then started the REAL worker process against the real dev DB/Redis and confirmed it boots, schedules all 3 real sources, and completes a real live ingestion cycle (8 new real Stories created from InsideEVs/Motor1's actual feeds, 0 errors) with the fix in place — no regression. No dedicated test suite for this file (same CLI-entrypoint-shape judgment already made for `packages/config`/the backfill scripts — verified live instead). **Real, severe gap found and fixed 2026-09-08 (same tick, a second instance of the identical bug)**: swept for the same "unhandled rejection kills a long-running process" shape immediately after finding the one above and found a second real instance in the same file — `worker.on("failed", handleJobFailure)` registered the async `handleJobFailure()` (which does its own real DB read+write to raise the CRITICAL exhausted-retries alert) directly as a plain `EventEmitter` listener. Verified live (a real subprocess) that `EventEmitter.emit()` doesn't await an async listener or catch its rejection any more than a bare `setInterval` callback does — a transient failure inside the alert-raising DB call itself would have crashed the whole worker from inside the one code path whose entire job is reacting safely to something already going wrong. Fixed the same way: wrapped in a listener that `.catch()`es and logs any rejection instead of letting it propagate. Verified live at every step, same methodology as the reconcile fix: a real subprocess reproducing the old pattern confirmed the crash, a real subprocess reproducing the fixed pattern survived repeated simulated failures, and the real worker process (against the real dev DB/Redis) still boots and schedules correctly with both fixes in place. Swept the whole codebase afterward for any other bare `setInterval`/`setTimeout`/`EventEmitter.on()` wrapping an unguarded async call — found and fixed a third, lower-stakes instance (the `SIGTERM` shutdown handler: `worker.close()`/`queue.close()`/`prisma.$disconnect()` rejecting would still crash before reaching a clean `process.exit(0)`, though the process is already mid-shutdown by then, not mid-operation) rather than leave a known gap once found; confirmed via a final sweep that all three in `apps/*` are now covered. **Minor observability gap found and fixed 2026-09-08, same tick**: checked the worker's own `ioredis` connection for the same class of risk and found something different — verified live (a real unreachable-address test) that `ioredis` does NOT crash the process on a genuine connection failure with no `error` listener attached (it has its own internal default handler for exactly this well-known Node `EventEmitter` gotcha), so no crash risk here. But that default handler logs an unstructured `[ioredis] Unhandled error event: ...` line straight to stderr, inconsistent with every other real error path in this file. Attached an explicit listener for consistency (not safety, already safe either way) — and caught a second real thing while verifying it: a genuine `ECONNREFUSED` here surfaces as a real `AggregateError` (Node's `net` trying both IPv6 and IPv4 loopback and wrapping both failures) whose own top-level `.message` is an empty string; the real detail lives in `.errors`. Verified this exact shape live before trusting the extraction, so the structured log line actually contains the real failure reason instead of an empty string. |
| Seed data (RBAC roles/permissions, markets, sources) | **Done** — 5 real Topics as of 2026-09-08 (was 3): checked every real unclassified Story (73 at the time — over half of everything ingested) against the existing 3 topic rules and found two real, sizeable clusters with nowhere to land — e-bikes/e-scooters (ENGWE, SONDORS, Macfox, Navee, Tenways, Velotric, Rivian's own ALSO e-bike) and autonomous/robotaxi (Waymo, Uber's UK launch, Tesla Cybercab). Added `micromobility`/`autonomous-robotaxi` to `packages/editorial/src/topic-classifier.ts`'s `TOPIC_RULES` (each keyword individually verified against every real unclassified title for false positives, same discipline as every existing rule's own comments) + the duplicated `TOPICS` literal in `packages/database/src/bootstrap.ts` (can't cross-import — `@automotive/database` can't depend on `@automotive/editorial`). One real, deliberate test behavior change: the existing `"does not over-match on a broader 'cfo'"` test asserted `Macfox X1S e-bike review...` classified as `null` — now correctly `micromobility` (its real "e-bike" mention), not a regression of the original guarantee (still correctly not `market-business`). Backfilled live against the real production DB: bootstrap upserted the 2 new Topic rows, then `backfill:topics` reclassified the real backlog — 9 real stories → `micromobility`, 5 → `autonomous-robotaxi`, 59 honestly still unmatched (a real mix of deals roundups, explainers, and new-model reveals with no safe, verified keyword rule yet — left unclassified rather than force a risky guess). Verified live: homepage/sitemap.xml both picked up both new sections automatically (no code change needed beyond the rule/seed data itself — the homepage already renders one section per real Topic with ≥1 story). |
| `AIProvider` abstraction + AI budget guard | **Done** (interfaces, a no-op test provider, and — added 2026-09-08 — a real `AnthropicProvider`; see the dated note at the end of this row for exactly what "real" does and doesn't mean yet). **Real gap found and fixed 2026-09-08**: `docs/ai-pipeline.md`'s own "Budget control" section documents `recordExecution()` as the real, symmetric other half of `assertWithinBudget()` ("every `AIExecution` row records `tokensIn`/`tokensOut`/`estimatedCostUsd`") — and `assertWithinBudget()`'s own doc comment already described calling it "afterwards", as if it existed — but no such function was ever implemented anywhere in the package; the only thing with that name was a same-named private helper inside `budget.test.ts`, written purely to create fixture rows for `assertWithinBudget()`'s own aggregation tests, never exported or callable by real pipeline code. Not the externally-blocked gap (that's a live provider needing a real API key) — pure bookkeeping plumbing every future stage will need regardless of provider, found by checking this package's own documented contract against what it actually exports, the same "enumerate the real thing and diff against the doc" technique used elsewhere this session. Fixed with a real `recordExecution()` in `budget.ts`, the test file's fixture helper now delegating to it instead of duplicating the write. 3 new tests (full-field write incl. a real `PromptTemplate` fixture for the optional prompt-versioning FK — cleaned up child-row-first, same FK-safe discipline as `/admin/cars`'s own test fixtures; a failed-execution write; a recorded execution immediately visible to `assertWithinBudget`'s own aggregation). **Real gap found and fixed 2026-09-08 (same day, one more pass)**: `assertWithinBudget()`'s final `dbSum + estimatedCostUsd` is a single plain JS addition — hits the classic `0.1 + 0.2 !== 0.3` floating-point imprecision for real, verified live and specifically isolated to this one addition (Postgres's own `SUM()` aggregate was checked separately and does NOT reproduce the same imprecision — a real `SUM()` over stored `$0.1`/`$0.2` rows correctly returns a clean `0.3`). Enough to flip a strict `>` comparison at a boundary that's mathematically exactly at the configured limit — either a false rejection (fails closed, just annoying) or, in the other direction, a false acceptance a hair over the real limit (the actual failure mode a budget guard exists to prevent, however tiny the real-money magnitude). Fixed by rounding to cent precision at the comparison point only, not touching how anything is stored — no bigger integer-cents schema migration needed for a feature nothing calls yet. **Caught a flawed first test attempt before trusting it**: the initial test recorded both `$0.1` and `$0.2` as prior spend and asserted with `estimatedCostUsd: 0` — that only exercises Postgres's own (already-clean) `SUM()`, not the function's real vulnerable addition; caught by deliberately reverting the fix and confirming the flawed test still passed without it. Corrected to record only `$0.1` as prior spend and pass `$0.2` as the new `estimatedCostUsd` — confirmed this combination genuinely fails without the fix (`spentUsd: 0.30000000000000004`) and passes with it, the standard "let it fail first" test-validity check. | **Larger documentation gap found and fixed 2026-09-08 (same day, one more pass)**: `docs/ai-pipeline.md` in full read entirely in present tense, describing the whole 9-stage pipeline (Classifier through Moderator), the kill switch, and the budget guard as if largely built and running -- checked directly rather than assumed and confirmed zero real code anywhere writes to or reads `AIJob`/`AIExecution`/`PromptTemplate`/`PromptVersion`/`AIAgentAction`, `assertWithinBudget()` has no real caller (nothing to guard yet), and `AI_KILL_SWITCH` is parsed as a flag but never actually checked anywhere -- there is no real job-picker loop to check it in. Added a clear correction at the top of that file stating plainly what's real today (Ingest, plus the non-AI parts of duplicate detection/clustering) versus what remains the intended design to build once a real provider API key unblocks it, rather than continuing to read as a retrospective of a system that doesn't exist yet. **Real gap found and fixed 2026-09-08 (later pass, same shape as the `recordExecution()` fix above)**: `docs/ai-pipeline.md`'s "Budget control" section documents `assertWithinBudget()`'s failure mode as "the job is left PENDING (not failed — it resumes once budget resets) and a `SystemAlert` (WARNING) is raised" — but nothing anywhere turned a thrown `BudgetExceededError` into either real effect; `assertWithinBudget()` only throws. Not the externally-blocked gap (a live provider), and not fixable "later once the provider exists" either — this is pure job/alert orchestration around a guard that's already real and already tested, the exact same category as `recordExecution()`. Added `handleBudgetExceeded(jobId, error)` to `budget.ts`: one transaction that sets the real `AIJob.status` back to `PENDING` (never `FAILED`) and creates a real `SystemAlert` (`severity: WARNING`, `source: "ai"`, the error's own message, `metadata` carrying `scope`/`spentUsd`/`limitUsd`/`jobId`). 1 new real integration test against the actual local DB (creates a job, marks it `RUNNING`, calls the new function with a real `BudgetExceededError`, confirms the job is back to `PENDING` and a matching alert row exists with the right fields) — `packages/ai` now 10 tests, not 9. Full regression clean (typecheck all 10 workspaces, 283/283 — up from 282). No E2E run: confirmed via grep that `@automotive/ai` is referenced only in `apps/worker/package.json`'s dependency list, not actually imported by any real `apps/worker` source file yet — dormant, same posture as the Citation-relations fix earlier (unit/integration coverage is enough for code nothing live calls yet). **Real gap closed 2026-09-08 (later pass, first real provider implementation — user chose Claude Haiku 4.5 for cost, with a per-request daily budget cap enforced via the existing guard above rather than a local/free model, after a real hardware+cost evaluation earlier this session)**: added `packages/ai/src/providers/anthropic-provider.ts` (`AnthropicProvider`, real `@anthropic-ai/sdk@0.124.0` dependency) — the package's first implementation of `AIProvider.complete()` that isn't a no-op. Defaults to `claude-haiku-4-5` (`DEFAULT_ANTHROPIC_MODEL`), the cheapest current Claude model, deliberately chosen since this pipeline will call it at real volume across every ingested Story; per-call/day/month spend stays bounded by the already-real `assertWithinBudget()` above, not by anything inside the provider itself, matching this file's own "provider stays a thin adapter" precedent (`NullProvider`). `responseSchema` (spec §87 structured output) passes through as `output_config: {format: {type: "json_schema", schema}}` — verified against the actually-installed SDK's own `JSONOutputFormat` type (`node_modules/@anthropic-ai/sdk/resources/messages/messages.d.ts`) rather than guessed, since `client.messages.parse()`'s Zod-only `parsed_output` path doesn't apply to a plain JSON-Schema object. A `stop_reason: "refusal"` throws instead of silently returning empty text. `embed()` throws by design — Anthropic has no embeddings API; `AI_DEFAULT_EMBEDDING_PROVIDER=openai` (already in `.env.example`) is the documented answer for embeddings, not built here. New `create-provider.ts` (`createTextProvider()`) reads `AI_DEFAULT_TEXT_PROVIDER`/`ANTHROPIC_API_KEY` from `packages/config` and returns a real `AnthropicProvider` when both are set, else falls back to `NullProvider` — same "safe by default, no live spend without an explicit key" posture as every other `AI_*` flag in this project. 5 new unit tests against a constructor-injected fake Anthropic client (success path incl. exact token/model passthrough, the `responseSchema`→`output_config` passthrough, the refusal-throws case, the no-text-block-throws case, the `embed()`-throws case) — all passing; typecheck clean across all 10 workspaces; `verify:env-example` still clean (no new env vars introduced). **Not yet live-verified against a real Anthropic API call** — no `ANTHROPIC_API_KEY` is configured in this environment yet — and **still has zero real caller**: no pipeline stage/orchestrator invokes `createTextProvider()` yet, so this closes the "only a no-op provider exists" gap specifically, not the larger "AI pipeline actually runs" gap (still blocked on both a real key AND a job-picker loop that doesn't exist — see `docs/ai-pipeline.md`'s own corrected top note). Once a real key is set, the honest next verification step is a real live smoke call (`assertWithinBudget` → `AnthropicProvider.complete()` → `recordExecution()`, the same three-function chain every real stage will use).
| AI Writer stage + first published Articles (`apps/worker/src/write-article.ts`, `GET /v1/articles/:locale/:slug`, `apps/web`'s `/articles/[locale]/[slug]` page) | **Done for a first, honest-minimum version, live-verified 2026-09-08**: the first real caller of `packages/ai`'s `AnthropicProvider` (see that row below) — picks a real Story with no English `Article` yet, drafts a headline/subtitle/keyTakeaway/3-6 body paragraphs via `claude-haiku-4-5` (structured JSON output), through the real `assertWithinBudget()`/`recordExecution()` guard (a real `AIJob` per Story), writes a real `Article` (`status: PUBLISHED`) + ordered `ArticleBlock` rows. **Real, live-discovered API constraint**: `output_config.format`'s JSON Schema only accepts array `minItems`/`maxItems` of 0 or 1 (a real 400 the first time this schema was ever sent) — the 3-6 paragraph range is expressed in the field's `description` text instead; documented in `AnthropicProvider`'s own comment. **Real deploy-process gap found and fixed the same day**: `infrastructure/docker/Dockerfile.worker`'s `COPY apps/worker ./apps/worker` bakes source into the image at build time — `/repo` inside a running container is NOT a live bind mount of the host — so editing a file on the VPS and re-running it via `docker compose exec worker npx tsx ...` silently ran the STALE baked-in copy from the last image build, not the edit (caught by a debug `console.error` that never printed, then confirmed via the container's own file mtime predating the edit). Fix is procedural, not code: any `apps/worker` (or `apps/api`/`apps/web`) source change requires `docker compose build <service> && ... up -d --force-recreate <service>` before a `docker compose exec` run picks it up — a plain `exec` alone never will. **Live-verified for real**: ran against the real production DB, 9 real Stories → 9 real published articles, each readable at its real `/articles/en/:slug` URL (e.g. `/articles/en/volkswagen-id-polo-ev-faces-months-long-waitlist-due-to-high-demand-vq09g5`), homepage/topic-page headlines now link to the real article when one exists (`GET /v1/stories`/`GET /v1/topics/:slug` both widened with a real `articles` include — the topics one was missed on the first pass, causing a real live 500 on every topic page, `story.articles[0]` on `undefined`, caught and fixed within minutes of the first real deploy, not by inspection). 3 new `apps/api` integration tests (404 for a missing slug, 404 for an unsupported locale, a real PUBLISHED-vs-DRAFT-visibility + real block-ordering test) — run scoped (`vitest run -t "GET /v1/articles"`) against the real production DB rather than the full suite, which assumes dev-only seed fixtures (a demo BMW car, `admin@dev.local`) that don't exist in production and would spuriously fail ~90 unrelated tests if run there — confirmed live afterward that no test-fixture rows were left behind. **Deliberately the honest minimum, not the full 9-stage design**: drafts straight from the Story's title/summary and its SourceArticles' short excerpts — no independent Researcher stage, no formal Fact Checker pass exist yet (see `docs/ai-pipeline.md`'s own corrected note). **Automated the same day, next pass**: this used to require a manual `npm run write:articles` invocation — refactored into an exported `runWriteArticleBatch()`, called on a real periodic timer from `apps/worker/src/index.ts` (default: up to 3 articles every 900s), same crash-isolation discipline as the existing `reconcile()` timer (a batch failure can't take down the whole worker). This is also the **first real `AI_KILL_SWITCH` enforcement** anywhere in the codebase — the timer is only scheduled if the switch is off AND a real `ANTHROPIC_API_KEY` is configured, logging plainly which of the two blocked it otherwise. Verified live: the real worker boot log now reads `Writer stage scheduled: up to 3 article(s) every 900s`. **Real gap closed the same day, next pass — `AUTO_PUBLISH` honored for real**: this Writer stage used to hardcode `status: "PUBLISHED"` regardless of `AUTO_PUBLISH` (real default `false`, "AI assisted, not autonomous" — the same principle already enforced elsewhere by the `ai_agent` Role deliberately lacking `PUBLISH_ARTICLE`), a real, direct contradiction the first time anyone checked. Now: `AUTO_PUBLISH=false` lands a real `DRAFT` Article; only an explicit `AUTO_PUBLISH=true` reproduces the previous auto-publish behavior. Since a `DRAFT` with no review path is just an invisible dead end, built the real review queue the same pass: `GET/PATCH /v1/admin/articles` (`UPDATE_ARTICLE`/`PUBLISH_ARTICLE`-gated, real offset pagination, a real `AuditLog` row per publish/reject) + a new `/admin/articles` page (status-filter tabs, Publish/Reject buttons, same zero-client-JS `<form method="post">` pattern as every other admin correction flow) linked from every admin page's nav. 2 new `apps/api` integration tests (list + publish + reject + the public-404-after-reject check, + a 404-for-nonexistent-id check) — 2 of them need `adminAgent`'s dev-seeded login and correctly don't run against production (same documented limitation as the `GET /v1/articles` tests above); **live-verified for real on production instead**: logged in as the real `admin@kite99.com`, ran a real Writer batch (confirmed it now lands as DRAFT, not published), listed it via the real `/admin/articles?status=DRAFT` page, clicked-equivalent (a real authenticated POST) the real Publish action, confirmed the article moved to PUBLISHED and became publicly reachable. **Caught and fixed a real test-hygiene incident in the same pass**: the first attempt at the admin-articles integration test failed against production (expected — no dev seed there) *before* reaching its own cleanup code, leaving two real orphaned test `Article` rows (`"Draft Awaiting Publish"`/`"Draft Awaiting Reject"`) live in the production database, visible on the real `/admin/articles` page. Caught immediately by noticing unexpected content while live-verifying the feature (not by a separate audit), root-caused, and deleted via a direct one-off script — see this row's own lesson: a test whose cleanup runs only after its own assertions is not safe to point at a database that also has to stay clean, and this project's fresh-per-scenario production database in particular. **Second real, severe bug found live in the same verification pass, unrelated to the queue itself**: every one of this app's ~24 admin POST route handlers' success-redirect silently pointed a real browser at `http://localhost:3000` instead of the real public domain — see the new dedicated row above, found specifically because this feature's Publish button was the first admin write action ever tested through the real deployed site rather than only `next dev`. **Real operational state, 2026-09-08, later same day**: the review queue worked exactly as designed and the Writer's periodic timer kept running unattended — by the time anyone checked back, 78 real `DRAFT` articles had accumulated (only 11 published by hand) — a real, honest consequence of "AI assisted, not autonomous" with no auto-approval, not a bug. User's explicit decision: leave the 78 for manual review, but flip `AUTO_PUBLISH=true` on the real production `.env` going forward so new Writer output publishes immediately again — verified live (a real manual `write:articles` run immediately after the flip published straight to `PUBLISHED`, no DRAFT). The review queue itself stays fully built and reachable at `/admin/articles` for whenever manual review is wanted again — this is a live operational toggle, not code removed. |
| Real SEO/data-quality sweep (user directly asked "will Google actually rank this?" while reviewing the live site) | **Multiple real, severe gaps found and fixed 2026-09-09, all live-verified, none by inspection alone.** (1) **Zero real published Articles were in the sitemap** — `apps/web/src/app/sitemap.ts`'s own original comment ("Article URLs join this once apps/web has article pages") was accurate when written but never revisited once the Writer stage and the real article page both later landed; confirmed live (`curl .../sitemap.xml \| grep -c articles` → 0 against 105 real published articles). Fixed with a new public `GET /v1/articles` list endpoint (locale/slug/publishedAt/updatedAt, no auth, capped at 5000 — sitemaps support up to 50,000 URLs, nowhere near that scale yet) + `getArticleSlugs()` + wiring into the sitemap at `priority: 0.9` (the site's actual news content, second only to the homepage). 1 new `apps/api` test. Verified live: sitemap.xml now lists all 105 real article URLs. (2) **Zero Open Graph or Twitter Card tags anywhere on the site** — real, meaningful both for social-share unfurls and as a fallback signal multiple crawlers use; confirmed live via curl before fixing. Fixed with a real site-wide default (`apps/web/src/app/layout.tsx`'s own `metadata.openGraph`/`.twitter`, plus a real `metadataBase` — Next's Metadata API merges a route's own returned object over the parent layout's, so a per-page override doesn't need to spread these) and a full per-article override (real headline/subtitle/image) in the article page's `generateMetadata()`. (3) **Zero `Article`/`NewsArticle` JSON-LD anywhere** — `apps/web/src/app/layout.tsx`'s own original comment had correctly deferred this ("join once those page types exist") but was never revisited once they did. New `packages/seo/src/article.ts`'s `buildArticleJsonLd()` (real headline/image/datePublished/dateModified/author/publisher, `author` naming the platform itself since no human byline exists for AI-written content — spec's own AI-transparency principle) wired into the article page alongside the existing BreadcrumbList. 4 new `packages/seo` tests. (4) **A real, live, visibly broken rendering bug, spotted by the user on a real screenshot**: `BYD&#8217;s new Defender-like SUV...` rendering as literal `&#8217;` instead of an apostrophe on the real homepage. Root-caused against electrek.co's own real live feed bytes (not assumed): the real RSS XML genuinely contains the standard numeric character reference `&#8217;`, and `fast-xml-parser`'s own defaults do not decode numeric entities without `htmlEntities: true` (confirmed live, before and after, with the installed version) — fixed in `apps/worker/src/rss.ts`, plus a new `backfill:entities` CLI (reuses the identical fast-xml-parser decoding logic, not a reimplemented entity table) that found and fixed 67 real already-ingested rows (33 `Story.title`, 34 `SourceArticle.title`) live in production. 1 new `apps/worker` test using this exact real headline as its fixture. (5) **~20 of 33 real free-stock (Wikimedia Commons) hero images were topically wrong despite passing every existing relevance filter** — found by the user manually reviewing real screenshots, then confirmed by reading all 33 real (headline, matched-file-title) pairs side by side: proper-noun collisions no keyword filter catches (`"Ford wants to sell 100,000 Fathoms..."` matched a photo of **Betty Ford** the person; `"DOT Set to Announce Fuel Economy Rules..."` matched **'Polka Dot Door'**, a 1980s TV show; `"US battery storage hits record..."` matched real **Civil War artillery** — "Battery M"; `"Federal Court Restores California's Clean Air Standards"` matched **Brazil's** Supreme Federal Court building). All 20 real bad `ArticleImage`/`Image` rows deleted directly (the affected articles now correctly show no image rather than a wrong one) — a real, honest limit of keyword-based matching without semantic/vision verification, not something this pass tried to re-solve algorithmically (already flagged as a known, deliberately-deferred gap — see that row above). Separately, spot-checked 3 real AI-generated (OpenAI) hero images by downloading and viewing them directly — all 3 genuinely accurate and professional-looking, confirming AI-generated images (created from the headline directly) carry much lower topical-mismatch risk than the Commons keyword-search path. **Net real state changed by this pass**: all 78 real DRAFT articles published (auditlog'd) per explicit user instruction, 20 bad images removed first — 105 real articles now live, 72 with a real hero image, 33 honestly without one. |
| Real free-stock hero images (spec's Image rights engine — `Image`/`ImageLicense`/`ArticleImage` models, previously 0% built) | **Done for a first, honest-minimum version, live-verified 2026-09-08** — user's explicit instruction: search free stock first, AI generation only as a fallback (the fallback isn't built — needs a real image-gen provider key not configured yet, this row is the free-stock half only). New `apps/worker/src/fetch-images.ts` searches Wikimedia Commons (no API key needed) per Article, filters to real photos (`image/jpeg`/`png`, ≥800px wide, excludes files with "logo"/"diagram"/"badge"/"icon"/"map" in the name) under an unambiguous license (`cc0`/`cc-by`/`cc-by-sa`/`pd` only — explicitly excludes NC/ND variants, since this is a commercial editorial site), hotlinks Commons' own CDN via a requested thumbnail width (never downloads/mirrors the original), and stores a real `Image` + reused-by-license-type `ImageLicense` + `ArticleImage(role: HERO)` row with real author/license/attribution metadata. Wired into `write-article.ts` (every new article gets a real search attempt) and a standalone `backfill:images` CLI for existing ones. New `GET /v1/articles/:locale/:slug` `images` include + a rendered `<figure>` with real required CC attribution on the article page; `GET /v1/stories`/`GET /v1/topics/:slug` also widened so the homepage/topic-list/admin-review-queue can show a real thumbnail. **Two real, severe relevance false-positives found live during the first production backfill, not by inspection** — both fixed by tightening the matcher, not by trusting a single pass: (1) a full AI-written headline (10+ words, generic filler) as the search query found ZERO usable images for 12/12 real articles at first, while the same headline's leading "Brand Model" substring alone found real, usable, correctly-licensed photos — Commons' search is plain full-text, not semantic, so a long sentence dilutes the match; fixed with `buildSearchQueries()` trying progressively shorter candidate queries (full text, first 4 words, first 2 words) until one lands. (2) Once queries got short enough to find results, some were flatly wrong despite passing every license/mime/size check — a "LED Technology Revives Classic Round Headlight Design" (about automotive headlights) matched a Commons file about LED *display* technology; a "Mercedes Patent Reveals..." (about a patent filing) matched an 1885 replica of the actual historic "Benz Patent Motorwagen" whose title happens to mention "Mercedes-Benz World" as a museum venue — both real, license-clean photos, both topically wrong. A license/mime/size filter can't catch a relevance problem. Fixed with a real word-overlap check (`isRelevantTitle()`: requires all of a 1-2-word query's words, or ≥half of a longer query's words, to appear as real whole words in the candidate's own title) — this alone still let the Mercedes-museum case through (both "Mercedes" and "Patent" are genuinely, literally present, just in an unrelated sentence), so added a second, positional check: the query's own first word (the brand, by construction) must appear within the leading half of the candidate title's characters, matching how every genuine match this session actually looks ("BMW iX3, IAA Summit 2025...", "Volkswagen, Auto 2024, Zurich...") versus the museum caption's "...is located at **Mercedes**-Benz World..." trailing near the very end. Verified live at each step by re-running the real backfill and reading the real attached rows, not by re-reading code: 10/12 (with ≥2 confirmed wrong) → 4/12 (fewer wrong, but the Mercedes/LED cases persisted) → 2/12, both independently confirmed correct (a real BYD/Denza group photo, a real Volkswagen Polo) — a real, honest, much lower hit rate traded deliberately for zero known false positives, since a wrong photo undermines a serious platform more than no photo does. **Real safety net added the same pass, not just a code fix**: `/admin/articles`'s review queue (already gated behind `AUTO_PUBLISH=false` — see the AI Writer stage row above) now shows the actual attached hero image before a human clicks Publish, since keyword-based relevance matching has a real, demonstrated ceiling no amount of regex tuning fully closes — a human actually seeing the photo is the last, real line of defense, matching this project's own "AI assisted, not autonomous" principle. `next.config.mjs`'s CSP `img-src` gained a **second** live-discovered gap the same pass: Commons' API returns thumbnail URLs on `thumb.wikimedia.org`, a different real subdomain than `upload.wikimedia.org` (the one first allowed) — every real hero image was silently CSP-blocked in a real browser until this was caught (by inspecting the real page HTML, not by browser-testing) and widened to `https://*.wikimedia.org` rather than enumerate subdomains one violation at a time. No dedicated automated test suite (network-dependent, real Commons API calls) — verified live throughout, matching this session's own established judgment call for CLI-entrypoint-shaped scripts. **Real AI-generation fallback built and live-verified 2026-09-09** (user's explicit follow-up decision, with a real `OPENAI_API_KEY` provided): new `apps/worker/src/generate-image.ts` calls OpenAI's `gpt-image-1` (`quality: "low"`, the user's explicit cost choice; `size: "1536x1024"`, one of exactly 3 real supported sizes — verified against the real OpenAI SDK's own response type before picking it, not guessed) only when `attachHeroImage()` finds nothing free, through the same real `assertWithinBudget()`/`recordExecution()` guard as the Writer stage (reuses `AIJobType.WRITE_ARTICLE` with a `purpose` marker in `input` rather than a new enum value — a real, deliberate simplification, not an oversight, to avoid a schema migration for a minor taxonomic distinction). **Real cost computed from the response's own real token usage**, not a flat per-image guess — verified live against OpenAI's actual published per-token rates ($5/1M text-input, $10/1M image-input, $40/1M output) before writing the code: a real first generation cost **$0.0164**. Stores the real generated PNG's bytes to a shared `uploads_data` Docker volume (`sourceType: AI_GENERATED`, `generatedByAi: true`, `rightsStatus: OFFICIAL_USE_ALLOWED` — no third-party rights question since it's the platform's own generated content) and registers it the same way as every other hero image (`Image`/`ArticleImage(role: HERO)`). Alongside this, a **manual upload path** also shipped the same pass (`POST /v1/admin/articles/:id/hero-image` + a real `<input type="file">` form on `/admin/articles`, real magic-byte sniffing rather than trusting the browser's claimed `Content-Type`) — for one-off replacements/a human's own picture, independent of the automated fallback. **Real, severe bug found live during the very first end-to-end test, not by inspection**: the generated image's DB row and file both existed correctly, but the URL 404'd on the real site. Root cause, confirmed by direct comparison (identical file, `docker compose restart web` alone flipped it from 404 to 200 with zero code change): Next.js's `next start` only serves `public/` files that existed at server boot — a file written to `public/` while the server is already running never becomes servable until the next restart, a real, previously-untested code path (this app never had a real runtime-written public asset before this feature). Manually restarting `web` after every single automated image generation isn't viable. Fixed by moving storage out of `public/` entirely to a sibling `apps/web/uploads/` directory served by a real, new `apps/web/src/app/uploads/[filename]/route.ts` Route Handler (`fs.readFile` fresh on every request — a real path-traversal guard on the filename, not decorative) — genuinely dynamic, no restart needed, verified live: a newly-generated image served `200` immediately with no restart in between. `apps/web/src/proxy.ts`'s middleware matcher also gained a real `uploads/` exclusion the same pass — every image load was otherwise paying a real, pointless `apps/api` redirect-lookup round trip plus a spurious analytics pageview per thumbnail. **Real, still-open relevance-quality gap, flagged live by the user with real screenshots, deliberately deferred (their own explicit "not now, for later")**: even after the Commons matcher's word-overlap + positional-anchor tightening (see above), a "Wisconsin ... $25M Federal Grant for 42 DC Fast-Charging Stations" article matched a 1940s county property survey map (whose title/description evidently contains "Wisconsin" positioned early enough and enough other coincidental term overlap to pass both checks), and a "Mullen Five Concept Car" article matched an unrelated media-panel photo (a proper-noun collision — "Mullen"/"Five" both genuinely present in that photo's own real title/description for reasons unrelated to the car). Both are real, honest limits of keyword-based matching without semantic/vision verification, not a regression of the fixes already made — logged here rather than re-tuned further this pass per the user's own instruction to leave it for later. |
| `apps/api` (auth+JWT, RBAC middleware backed by real DB permission checks, audit logging, health/ready, public reads, one gated write) | **Done, tested end-to-end live** (login → cookie → `/me`; unauthenticated PATCH → 401; authenticated admin PATCH → 200 + real DB update + audit_logs row). **Real, high-severity gap found and fixed 2026-09-07**: `requirePermission()` (`apps/api/src/permissions.ts`) and `GET /v1/auth/me` both checked only whether a `UserRole` grant existed — neither ever checked `User.status`. `PATCH /v1/users/:id/status` (the real "Suspend" button on `/admin/users`, built and E2E-tested the same day) only ever updates `User.status`; it never touches `UserRole`. Combined with a real 7-day JWT session (`auth.ts`'s `JWT_EXPIRY`), the verified, actual behavior was: suspending a user did nothing at all for anyone who already had a live session — every permission check, and the admin dashboard itself, kept working exactly as before, for up to 7 real days. `POST /v1/auth/login` already correctly refused a non-ACTIVE user a *new* session; the gap was purely that nothing ever revoked an *existing* one. Fixed by folding a `user: { status: "ACTIVE" }` condition into `requirePermission()`'s existing query (one round trip, not a second lookup) and adding the same check to `GET /v1/auth/me`. Verified live, not just via the test suite: created a real editor user, logged in as them in a separate real session, confirmed real access (`GET /v1/auth/me` 200, `POST /v1/brands` 201) via curl, suspended them as admin through the real API, then reused the *exact same, never-reissued* session cookie — `GET /v1/auth/me` now genuinely 401s and `POST /v1/brands` now genuinely 403s. 1 new `apps/api` integration test proving the same thing end to end (a second real login, a real permission check before and after suspension, same untouched session cookie throughout). **Real gap found and fixed 2026-09-07 (same day, later pass, insecure default JWT secret)**: `packages/config`'s `AUTH_JWT_SECRET` had a hardcoded, publicly-visible-in-source default (`"dev-only-insecure-secret-change-me"`) and no length validation at all — starting this service in production without that env var explicitly set would boot successfully and silently sign every real session with a secret anyone reading this codebase already knows, letting anyone forge a valid token for any user (including an admin) and bypass every auth/RBAC fix made today, including the suspension fix directly above. `jsonwebtoken`'s own defaults were separately confirmed safe first (an `alg:none` forged token is correctly rejected without needing an explicit `algorithms` allowlist — verified live with a real forged token, not assumed), so this was specifically about the secret's own strength, not a library-level bypass. Fixed with a startup check, same "fail loud in production, never silently insecure" posture as the session cookie's `secure` flag: throws immediately if `NODE_ENV === "production"` and `AUTH_JWT_SECRET` is either still the literal default or under 32 characters. No dedicated test suite exists yet for `packages/config` (no vitest wired up there) — standing one up just for this one guard was judged disproportionate scope, so verified live instead via 4 real subprocess runs covering every real combination: production+default (throws with the exact real error text), production+a real 32+ char secret (boots), development+default (boots, unaffected — confirmed no script anywhere sets `NODE_ENV=production` during dev/test/E2E), production+a short custom secret (also throws). **Real gap found and fixed 2026-09-07 (same day, later pass, unguarded seed script)**: `packages/database/src/seed.ts` upserts a hardcoded, publicly-visible admin credential (`admin@dev.local` / `dev-admin-password`, printed to the console on every run) — its own existing comment already said "never run against a production database," but nothing enforced that, the same gap shape as `AUTH_JWT_SECRET` before its own fix directly above. Fixed with the identical posture: throws immediately if `NODE_ENV === "production"`, before any DB write. Verified live, not assumed: ran the real seed script twice — once with `NODE_ENV=production` (refuses immediately with the real error text, confirmed no DB rows touched) and once normally (`Seeded: 17 permissions, 3 roles, 4 markets, 3 sources, 3 topics` — identical output to every prior run this session, confirming the normal dev workflow this whole session has relied on is completely unaffected). **Real test-coverage gap found and fixed 2026-09-07 (same day, one more pass)**: enumerated every registered route in `apps/api/src/app.ts` and diffed against this file's own test coverage — `GET /v1/roles` (real, actively used to power the role dropdown on `/admin/users`' "Add user" form) and `POST /v1/auth/logout` (had real E2E coverage via the actual browser button, but no direct integration test at all, unlike every other endpoint here) both had zero dedicated tests. Added 3 new tests: `GET /v1/roles` unauthenticated-401 and returns-the-real-seeded-roles, and a real login→logout→same-cookie-now-401 round trip for logout. **Documentation gap found and fixed 2026-09-07 (same day, one more pass)**: `docs/deployment.md` never mentioned either of this day's two production-only startup guards (`AUTH_JWT_SECRET`'s throw, the seed script's refusal) at all — a deployer following that doc's own CI/CD section as written would hit an undocumented startup crash the first time they deployed without having read this README row first. Added a new "Required production environment variables" section there naming both explicitly, with the exact real conditions each one enforces. **Real, severe gap found and fixed 2026-09-08**: `packages/config`'s 7 feature-flag fields (`AUTO_CLUSTER`/`AUTO_RESEARCH`/`AUTO_DRAFT`/`AUTO_PUBLISH`/`AUTO_MODERATION`/`AUTO_SEO`/`AI_KILL_SWITCH`) all used `z.coerce.boolean()` — verified live (`node -e`) before touching anything that this coerces via JS's own `Boolean()` constructor, which treats ANY non-empty string as `true`, including the literal string `"false"`; only a genuinely empty string coerces to `false`. Every one of these flags is documented in `.env.example` as "all default OFF until proven safe", shown there with the exact literal value `AUTO_PUBLISH=false` — meaning a real deployment that copies `.env.example` as its starting `.env` (the normal, expected workflow) would have every one of these safety-off flags silently flip to ON, including `AUTO_PUBLISH` and `AI_KILL_SWITCH` itself, the one flag whose entire purpose is an emergency off-switch. Unlike `AUTH_JWT_SECRET`'s own production-only guard above, this didn't even require a mistake to trigger — writing `false` is the correct, intuitive thing to type, and it was silently wrong in every environment (dev/test/production alike). Fixed with a real true/false string parser (`booleanEnvVar()`: only the literal `"true"`/`"1"` is `true`, everything else — `"false"`/`"0"`/empty/unset-with-a-default — is `false`), replacing all 7 fields. Confirmed via grep this was the only `z.coerce.boolean()` anywhere in the codebase. No dedicated test suite exists for `packages/config` (same judgment call as the `AUTH_JWT_SECRET` guard above) — verified live instead via 5 real subprocess runs against the freshly-rebuilt `dist/` (unset→false, explicit `"false"`→now-correctly-false, `"true"`→true, `"1"`→true, `"0"`→false), plus confirmed a real `apps/api` dev server still boots cleanly against the fixed config. `featureFlags` has no real caller in `apps/*` yet (found before it could ever silently bite in production, not after). **Real gap found and fixed 2026-09-08 (same investigation, one more pass)**: the same file's numeric env vars (`AI_DAILY_BUDGET_USD`/`AI_MONTHLY_BUDGET_USD`/`AI_PER_TASK_MAX_USD`/`API_PORT`/`WEB_PORT`, all plain `z.coerce.number().default(x)`) have a related, lower-severity gotcha — `.default()` only applies when the var is completely unset, not when it's an explicitly empty string (`Number("")` is `0`, not the intended default). Verified live for the concrete real consequence: Node's own `http.Server.listen(0)` doesn't error, it silently binds a random OS-assigned port — a real, confusing deployment failure mode for `API_PORT=`/`WEB_PORT=` specifically (a reverse proxy or Docker port mapping expecting 4000/3000 would just never reach the service); for the budget fields, an empty value fails closed (blocks all AI spend) rather than open, so lower stakes than the boolean gap, but the same "silently not the default you expected" shape. Fixed with a shared `numberEnvVar()` preprocessor treating an empty string the same as unset; a genuine typo (e.g. `"abc"`) still correctly throws, confirmed unaffected. Verified live via 4 more real subprocess runs against the rebuilt `dist/` (unset→default, empty→now-correctly-default, valid value→respected, typo→still throws) plus a second real `apps/api` boot confirming it actually listens on the documented port 4000. **Real gap closed for good, 2026-09-08**: both of the fixes above were only ever caught by manually diffing `packages/config`'s schema against `.env.example` by hand -- the exact same "nothing enforces this automatically" shape as the Dockerfile build-order drift (see the Docker Compose row below). Closed the same way: new `infrastructure/verify-env-example.js` parses the real schema keys straight from `packages/config/src/index.ts` (never hand-copied) and confirms every one has a matching `.env.example` entry -- wired in as `npm run verify:env-example` and a new CI step right after the Docker build-order check. Verified it actually catches real drift, not just that it passes today: removed `AI_KILL_SWITCH=` from a backup copy of the real `.env.example`, confirmed the script fails with that exact missing key named, then restored the real file and diffed it back to byte-identical before moving on. **Real test-coverage gap found and fixed 2026-09-08**: `attachSession` (`permissions.ts`) runs on EVERY single request via `app.use()`, not just authenticated routes, and its own `verifySession()` (`auth.ts`) already has a real `try/catch` around `jwt.verify()` specifically so a malformed/expired/tampered session cookie is treated as "not logged in" rather than a thrown error — but nothing had ever exercised that catch path. Had it ever been accidentally weakened (e.g. calling `jwt.verify` directly, without the try/catch), a single garbage cookie value would turn into an unhandled exception on every request site-wide — the same severity class as the `marketId` process-crash bug fixed earlier this session, just guarded here from the start rather than found broken. Added 2 new tests: a malformed cookie on an auth-required route (`/v1/auth/me`) correctly 401s, not 500s; the same malformed cookie on a public route (`/v1/sources`) still 200s, confirming `attachSession`'s global reach doesn't itself break routes that never needed a session. Both passed on the first run, confirming the existing guard was already correct — this closes a real, previously-uncaught regression risk on a security-critical path, not a behavior change. **Real test-coverage gap found and fixed 2026-09-08 (same day, one more pass)**: `GET /ready`'s only existing test confirmed the happy path (a real, working DB connection) — the entire actual PURPOSE of a readiness endpoint (an orchestrator/load balancer using it to decide whether to route real traffic here) is detecting when the DB is genuinely unreachable, which had never been exercised at all. Added 1 new test mocking `prisma.$queryRaw` to reject for just that one test (can't take down the real shared dev DB without breaking every other test in this file) — confirms a real `503 not_ready` with the real error message, not a thrown exception or a false `200`. **Documentation gap found and fixed 2026-09-08 (same day, one more pass)**: `docs/security.md`'s "Secrets" section claimed "log redaction for any field matching `*_KEY`/`*_SECRET`/`*_TOKEN`/`*PASSWORD*`" as if it were real — checked directly, not assumed: no such redaction logic exists anywhere. Investigated the specific concern that claim seemed to guard against — `GET /ready` puts a real DB-connection error's message directly in its public response body, so if that message ever embedded the real `DATABASE_URL` password, anyone could read it during an outage — by reproducing both realistic Prisma connection-failure shapes (host unreachable, wrong credentials) against a real Prisma client: neither includes the actual password in its error text, only username/host. So the specific path is currently safe, but the documented safeguard itself was never built (nothing today logs a dynamic keyed object a field-name filter could even apply to). Corrected the doc to state this accurately rather than leave a false safety claim on record. **Larger documentation gap found and fixed 2026-09-08 (same day, one more pass)**: `docs/architecture.md`'s "API endpoints (MVP surface)" section — a flat route list with no "not yet built" framing — was checked directly against `apps/api/src/app.ts`'s real registered routes (44 total) for the first time this session. Almost none of it still matched: the whole Articles/Comments/AI-operations sections were never built (no `Article`/`Comment` rows exist, no AI pipeline to have jobs/a kill-switch endpoint), `GET /v1/stories/:id` and `POST /v1/sources/:id/trigger` specifically were never built either — while roughly 30 real, live, tested routes this session actually built (brands, generations/trims/engines/batteries, redirects, alerts, audit log, user management, markets, topics, analytics, search-query observability) are entirely absent from the list. Deliberately did NOT rewrite the list to match current reality — this file's own "What's actually built" section already learned the hard way (three same-day corrections) that a hand-maintained summary of a fast-changing admin surface rots quickly; an endpoint list would rot identically. Instead added a clear correction labeling the section as the original early-project planning sketch, kept for historical context, and pointing at `apps/api/src/app.ts` itself (or this README's own exhaustive per-endpoint documentation) as the real, current source of truth. **Real, low-severity gap found and fixed 2026-09-08**: audited every public GET list endpoint for whether a client controls the page size (following on from the several offset-pagination-hiding-real-data bugs already found and fixed this session on `/v1/stories`/`/v1/alerts`/`/v1/audit-log`/`/v1/search-queries`). `GET /v1/cars`, `GET /v1/brands`, `GET /v1/topics`, `GET /v1/markets` and `GET /v1/sources` all return every real row with no `limit`/`offset` at all — currently harmless (each table has a handful of real rows, all created deliberately by an admin, not automatically multiplying the way audit-log/analytics/search-query rows do), but a real, honest future-scale limitation worth naming rather than leaving silent: since this platform's own stated purpose is a *growing* automotive knowledge graph, these five endpoints will eventually need the same real offset-pagination treatment already applied elsewhere this session. Documented rather than implemented now — matching this project's own established precedent (see the `/v1/redirects/lookup` per-request-DB-call note above) of not adding pagination/caching machinery ahead of the data volume that would actually justify it. **Real gap closed 2026-09-08 (production admin creation, requested for a real upcoming deploy)**: `docs/deployment.md` had explicitly flagged "the real production admin account(s) still need to be created deliberately... not documented here yet since no such process has been built." Built it for real: `packages/database/src/seed.ts` used to define permissions/roles/markets/sources/topics directly, right next to its own `NODE_ENV=production` refusal — but that data (Role/Permission/Source/Topic rows) is real production infrastructure, not dev fixture data, and would have been unreachable in production if left inside the refusing file. Extracted it into `bootstrapPlatformData()` (`packages/database/src/bootstrap.ts`, no production guard, safe anywhere) with its own CLI entry point (`npm run db:bootstrap`); `seed.ts` now calls it and keeps only what's genuinely dev-only (the demo BMW knowledge-base rows, the hardcoded `admin@dev.local` credential) behind its existing guard. New `packages/database/src/create-admin.ts` (`npm run db:create-admin`) reads `ADMIN_EMAIL`/`ADMIN_PASSWORD` (rejects under 12 characters)/optional `ADMIN_NAME` from the environment — nothing hardcoded, password never logged — and refuses with a clear error if the `admin` Role doesn't exist yet rather than silently creating a roleless user. Verified live against the real local DB: `db:seed` re-run confirmed byte-identical bootstrap output to before the refactor; `db:create-admin` refused correctly with no env vars set and with a too-short password; a real run with valid test credentials created a genuine `User` (confirmed via a direct query: `status: ACTIVE`, `roles: ["admin"]`, and the stored hash verified against the real password via `bcrypt.compare`), then cleaned up. Full regression clean: typecheck all 10 workspaces, 283/283 (no dedicated tests for setup scripts, matching how `seed.ts` itself was already verified — live runs, not the automated suite). |
| Security response headers (`apps/api` + `apps/web`) | **Done, unit + E2E + live-verified, including a real CSP** — real gap found 2026-09-07: curl showed zero security headers beyond framework defaults on both services, and both actively advertised their stack via `X-Powered-By`. Fixed with `helmet` (a standard, audited library, not hand-rolled — no environment constraint here forcing a custom version, unlike rate limiting) on `apps/api`; `next.config.mjs`'s `poweredByHeader:false` + a `headers()` function on `apps/web`. **`Content-Security-Policy` added same day** (initially deferred as needing careful testing — came back and did that testing): researched Next.js 16's own current docs for the exact tradeoff (nonce-based CSP forces every page into dynamic rendering — a real regression given this app has legitimately static pages) and chose the official "without nonces" form instead, since this app has zero external scripts/analytics — everything is same-origin. Verified with a real headless Chromium instance (not assumed): loaded every real page type in both `next dev` *and* a real production `next build && next start`, with console monitoring for CSP violations, including the one fetch-based admin login flow — **zero violations in either mode**, confirmed static pages stayed static in the production build output. Locked in with `apps/api`/`apps/web` integration + E2E tests; full E2E suite (all 38 tests incl. accessibility) passes with the CSP active. **Real security gap found and fixed 2026-09-07 (fallthrough error handling)**: `apps/api` had no catch-all 404 route and no error-handling middleware at all. Confirmed live before fixing: an unmatched route hit Express's default HTML 404 page, and — more seriously — a malformed JSON request body (a real, attacker-triggerable input) hit Express's default error handler, which returned an HTML page containing a full stack trace **with real absolute server filesystem paths** (`C:\...\node_modules\body-parser\...`) — a genuine information-disclosure bug, not cosmetic. Fixed with a catch-all 404 handler and a final error-handling middleware, both returning this API's own established `{error: "..."}` JSON contract instead of Express's HTML defaults; the error handler distinguishes a bad-JSON body (`400 invalid_json`) from anything else (`500 internal_error`), logs the real error server-side via the existing correlation-id logger, and never echoes any error detail to the client regardless of type. Verified live via curl (both cases confirmed fixed) before writing the 2 new integration tests (one asserting the response contains none of `node_modules`/`SyntaxError`/a `.js:<line>` reference — a real regression guard against ever leaking a stack trace again, not just a status-code check). **Real gap found and fixed 2026-09-07 (same day, later pass, missing HSTS)**: every other real security header on `apps/web` was added the same day this app's headers were first audited, but `Strict-Transport-Security` was missed entirely — found by comparing live curl output from both real services side by side: `apps/api` already sends a real HSTS header (one of `helmet()`'s own defaults), `apps/web` — the actual public-facing site real browsers load — sent none. Fixed in `next.config.mjs` with the same value `apps/api` already sends, unconditional (harmless in dev — a browser only starts honoring/caching HSTS after seeing it over a real HTTPS connection). Verified live via curl on the real dev server before and after. 1 new Playwright E2E assertion added to the existing security-headers test. **Real gap found and fixed 2026-09-07 (same day, one more pass, full header parity)**: went back and did a full head-to-head of every real header both services actually send, not just the one already found missing — caught 7 more real ones `helmet()` sends by default with no equivalent on `apps/web` at all: `Cross-Origin-Opener-Policy`, `Cross-Origin-Resource-Policy`, `Origin-Agent-Cluster`, `X-DNS-Prefetch-Control`, `X-Download-Options`, `X-Permitted-Cross-Domain-Policies`, `X-XSS-Protection`. The first two are genuinely meaningful current process-isolation hardening (Spectre-class cross-origin attack mitigation), not legacy boilerplate — `same-origin` is safe for both given this app has zero external scripts/embeds. Added all 7 to `next.config.mjs` with the same values `helmet()` already sends. Verified live via curl on the real dev server (all 7 present with correct values). 1 new Playwright E2E test asserting all 7 at once. **Real test-quality gap found and fixed 2026-09-08**: swept every security-header test in both `apps/api` and `apps/web` for weak `toBeDefined()`/presence-only assertions sitting next to sibling headers that already got an exact-value check — found 3 real instances. `apps/api`'s own `x-frame-options` test only checked presence, unlike its neighbor `x-content-type-options` on the very next line — strengthened to the real value (`SAMEORIGIN`, verified live). `apps/web`'s E2E `permissions-policy` test had the same gap, unlike every other header in that same test — strengthened to the real configured value (`camera=(), microphone=(), geolocation=()`, verified directly against `next.config.mjs`). `apps/api`'s rate-limiter test checked only that a `Retry-After` header existed, not its value — since that test already uses fully deterministic fake timers, strengthened to the exact expected value (`60`, the full window length) rather than a loose "some positive number" bound. Each of these would have passed unchanged through a real regression (a wrong/unsafe header value, or a nonsensical `Retry-After`) — no code behavior changed, only the tests' own ability to catch a real regression. **Same day, two more passes on this exact technique**: swept every `expect(audit).not.toBeNull()`-style AuditLog check in `apps/api/src/__tests__/api.test.ts` (12 total) and found a real, consistent split — the 6 "correction" tests already bake the real `action` string into the query itself (e.g. `action: "update_brand"`, so a wrong action would make `findFirst` return `null` and correctly fail), but all 6 "create" tests omitted `action` entirely, so any audit row for that entityType/entityId — including one logged with the wrong action string — would satisfy `not.toBeNull()`. Added the real action (`create_brand`/`create_car_model`/`create_generation`/`create_trim`/`create_engine`/`create_battery`, each verified directly against `app.ts`) to all 6, matching the already-correct convention. Separately, `apps/web`'s own admin E2E market-picker test checked only that `Fact.marketId` was non-null after selecting "United Kingdom (GB)" — the page's own "(GB)" display check right above it already proves the UI reflects a market, but not that the CORRECT one reached the database (display and storage logic could diverge). Strengthened to compare against the real GB `Market` row's own id directly. **Real, severe gap found and fixed 2026-09-08 — the most severe found this whole session**: Express 4 does NOT catch a rejected Promise thrown by an `async (req, res) => {...}` route handler (every route in this file is exactly this shape) — verified live, not assumed: POSTed a real fact with a bogus `marketId` to a running dev server and watched a real Prisma foreign-key violation become an uncaught exception that killed the entire Node process (confirmed dead via `/health` going unresponsive for every other in-flight request afterward, not just the offending one — a real, authenticated-editor-triggerable full outage, not a contrived edge case). Fixed with `express-async-errors` (a well-known, single-purpose, one-line shim for exactly this Express-4-era gap — monkey-patches the router so an async handler's rejection reaches the existing error-handling middleware instead of crashing the process; imported at the very top of `app.ts`, before any route is registered, deliberately not a custom try/catch-everywhere rewrite). See the "car fact correction" row for the specific `marketId`-existence-check half of this same fix. 1 new `apps/api` integration test proving both the clean 404 AND that the process itself is still alive and serving other requests afterward. |
| `qs` CVE mitigation (`apps/api`, `npm audit` finding) | **Real vulnerability found, root-caused, and mitigated 2026-09-07** — `npm audit` had flagged 2 moderate `qs` CVEs (via `express`→`body-parser`) all session without ever being investigated. No safe upgrade exists: `body-parser@1.20.6` pins `qs: "~6.15.1"`, and the only release past the vulnerable range requires `body-parser@2.x` + **Express 5** — a real breaking major-version migration, correctly *not* attempted inside this tick. Mitigated instead: Express's default `'extended'` query-string parser is what actually invokes `qs`; switched to `app.set("query parser", "simple")` (Node's own `querystring.parse`, never touches `qs`). Verified every real `req.query` usage in the app (search/stories/alerts/redirects) is a flat single value, not `qs`'s nested/array syntax, so this has zero functional impact — confirmed live via curl on all four endpoints post-fix. Honestly documented: `npm audit` still reports the CVE afterward (it reads the dependency graph, not runtime usage) — this eliminates real exploitability in this app, it doesn't silence the finding. Locked in with a real test asserting the parser setting; the actual fix (Express 5 migration) is tracked as a real, deliberate future migration, not smuggled in here. |
| Rate limiting on login + search + analytics events (spec §48, `docs/security.md` "Rate limiting & anti-spam") | **Done, unit + live-verified** — real gap found 2026-09-07: the spec explicitly names login/search/comment/contact as needing rate limiting, and none existed. Added `apps/api/src/rate-limit.ts`, a real in-memory fixed-window limiter (documented simplification: not Redis-backed — see project memory's BullMQ/Redis-version note — so state doesn't share across multiple server instances; that's the honest MVP tradeoff, not hidden). Applied: login 10/15min per IP, search 30/min per IP. `app.set("trust proxy", 1)` added so `req.ip` resolves to the real client behind the documented single-nginx-hop production topology, not the proxy's own address. 3 unit tests (fake timers: allows-then-429s, tracks IPs independently, window resets) + verified live via curl (11th login attempt in the window really 429s with a `Retry-After` header; 31st search really 429s). Comment/contact-form limiting will join once those endpoints exist for real. **Real gap found and fixed 2026-09-07 (same day, later pass)**: swept every real public write endpoint for the same missing-protection shape and found a third one this section's own "current state" accounting had missed entirely — `POST /v1/analytics/events`, hit automatically on every real page navigation, had no rate limit at all. Fixing it surfaced a subtler bug baked into naively copying the login/search pattern: this endpoint is only ever reached server-to-server from `apps/web/src/proxy.ts`'s internal call to `apps/api`, not through the real nginx hop the existing `trust proxy: 1` setup assumes — so a plain per-`req.ip` limiter would have collapsed every real visitor onto `apps/web`'s own server address, either doing nothing or throttling the entire site's real traffic the moment usage crossed the threshold. Fixed by having `proxy.ts` relay the real client's own `X-Forwarded-For` (the value nginx already set reaching `apps/web`) unchanged on to `apps/api`, so the new 120/min-per-IP limit genuinely keys on distinct real visitors — verified directly, not assumed: a test simulating 121 requests from one forwarded IP gets a real 429 on the 121st while a different forwarded IP in the same run is unaffected. 3 new `apps/api` integration tests (public-and-records-a-real-row, invalid-body-400, the two-distinct-IPs rate-limit proof). `docs/security.md` corrected to name all three real limiters instead of two. |
| Structured request logging + correlation id (`docs/security.md` "Observability") | **Done, unit + live-verified** — another real, documented, completely-unimplemented gap found the same pass: `apps/api` had zero request logging (only a startup line). Added `apps/api/src/request-logger.ts`: one JSON line per request to stdout (timestamp/correlationId/method/path/status/durationMs) — plain `console.log(JSON.stringify(...))`, not a new logging-library dependency, since that's the simplest option that actually satisfies "structured JSON logs" (a real aggregator reads stdout regardless of what produced it). Reuses an incoming `X-Request-Id` if the caller already set one, generates one (`crypto.randomUUID()`) otherwise, always echoes it back as a response header. 2 integration tests + verified live via curl (auto-generated UUID in the header + matching log line; a supplied `X-Request-Id` echoed back unchanged). |
| Admin panel POST-redirect base URL (all ~24 `/api/admin/*` write route handlers) | **Real, severe production bug found and fixed 2026-09-08 — the first time any admin write action was ever exercised through the real public domain + nginx rather than only `next dev`/E2E's direct localhost access.** Every route handler built its success/error redirect as `NextResponse.redirect(new URL(path, req.url))` — verified live via curl against the real deployed site (not assumed): `req.url` resolves to this container's own internal bind address (`http://localhost:3000`) behind nginx, not the real public domain the browser is actually talking to. Every single admin form submission — Publish/Reject an article, Resolve an alert, correct a fact, add a source, log out, all ~24 of them — silently redirected a real browser to an address it can't reach; the POST itself succeeded (the underlying data change happened), only the "take me back to see it" redirect failed, so the button looked broken even though it wasn't. Fixed once, centrally: new `adminRedirectUrl(path, req)` in `apps/web/src/lib/admin-auth.ts` uses the real configured `PUBLIC_URL` as the redirect base when it's been set to something real (checked against `.env.example`'s own literal placeholder default, `https://DOMAIN.COM`), falling back to the request's own origin otherwise — correct for local dev/E2E, which access the app directly with no reverse proxy in front of it, so those stay unaffected. Applied to all 24 call sites via a scripted sweep (never hand-copied, so none were missed) — every file re-typechecked clean afterward. **Found specifically while live-testing the new article review queue below** — a real example of why this session's discipline of testing every new feature against the actual public deployment, not just `curl`ing JSON APIs, keeps paying off. Not yet covered by a dedicated regression test (this app's Playwright E2E suite runs against a local dev server, which never reproduces the proxy-origin mismatch) — a real, honest gap; the fix is verified live instead (see the article review queue row's own live-verification notes). |
| `apps/web` homepage (Next.js 16, Server Component fetching `apps/api` live) | **Done, verified via real SSR HTML output + E2E** — a ranked "Latest" feed (`computeRankingScore()`), plus one real section per Topic underneath (spec §33/§66's Breaking/EV/Market/... layout, in its honest current form) — only renders a section if it has ≥1 real classified Story, no fake empty boxes. Cars/Tech/Trending sections still wait on more topic rules/entities existing. **Real robustness gap found and fixed 2026-09-08**: `getTopicSections()` fetched every real Topic via `Promise.all` — a single topic's fetch failing (a real, plausible transient issue; `getTopic()` throws for any non-404 failure) took down the ENTIRE homepage, including the "Latest" feed above it, which is completely independent and had already succeeded. This function already treats an absent topic the same as an empty one (filtered out, no fake box) — extended that same tolerance to a topic whose fetch failed, rather than leaving it as the one thing that could still crash the whole page. Fixed with `Promise.allSettled`, logging a rejected fetch server-side (not silently swallowed) and otherwise treating it like "nothing to show for this topic right now." Verified the exact logic live with a real throwaway script (one rejecting, one null, two successful — confirms partial success returned, no throw, the failure logged) before trusting it; full regression re-run clean (269/269 unit tests, real `next build`, 66/66 E2E, no change in outcome under normal all-succeed conditions). |
| Automotive Knowledge Base data (real BMW 3 Series/G20/330i·M340i·M3 Competition + engines + a time/market-scoped Fact) | **Done, seeded and verified in the real DB** |
| Car page `/cars/:brand/:model` (`apps/api` `/v1/cars/:brandSlug/:modelSlug` + `apps/web` page) | **Done, tested end-to-end live** (real SSR HTML shows all 3 trims/engines/the Fact; a nonexistent car correctly 404s). **Real gap found and fixed 2026-09-07**: `packages/utils`'s real, unit-tested locale-aware unit formatting (`formatPowerKw()`/`formatDistanceKm()`, spec §93-94 — "render one value per market, not the raw stored number with a hardcoded unit") had zero callers anywhere in `apps/web` or `apps/api`. This page was showing both hp AND kW simultaneously with hardcoded literal suffixes instead of the one real value the spec's own worked example calls for. Fixed with `formatEnginePower()`/`formatBatteryRange()` helpers using `"imperial"` (both of this project's two real seeded markets, US and GB, are `unitSystem: "imperial"` — not an arbitrary guess, the only two real markets that exist today), falling back to whichever raw field is present when the canonical one (kW/km) is missing since both are optional in the schema. Required adding `@automotive/utils` as an actual declared dependency of `apps/web` (it worked without one via npm workspace hoisting, but that's not something to rely on) — which meant re-verifying `Dockerfile.web`'s build order again (now the fourth real dependency-graph re-check this file has needed the same day), simulated locally in the exact `RUN` sequence to confirm. Locked in with a real E2E assertion against the real seeded B48 engine: shows "255 hp", no longer "190 kW" anywhere on the page. **Real gap found and fixed 2026-09-07 (Brand.country)**: `Brand.country` (ISO 3166-1 alpha-2, real seed data since the schema's original scaffold — BMW = "DE") already flowed through `GET /v1/cars/:brandSlug/:modelSlug`'s unshaped Prisma response, but `apps/web`'s own `CarModelDetail` type narrowed it away before any page could render it. Fixed by widening the type and adding `packages/utils`'s new `formatCountryName()` (backed by the platform's own `Intl.DisplayNames` rather than a hand-maintained code→name table — 3 real unit tests, including a genuine unresolvable-code fallback case). Car page now shows "BMW · Germany" instead of just "BMW". Verified live via curl against a real dev server; locked in with a real E2E assertion. Full regression sweep clean: 160/160 unit+integration (3 new), 53/53 E2E (extended, not duplicated). **Real gap found and fixed 2026-09-07 (same day, one more pass)**: `formatCountryName()` used `Intl.DisplayNames.of()` directly on whatever case the stored code happened to be — verified live (`node -e`), not assumed: `.of("DE")` → "Germany", but `.of("de")` (lowercase) silently returns the literal string `"de"` back unresolved, without throwing (only a genuinely malformed code like `.of("d")` throws, which the existing fallback already handled). Neither `POST` nor `PATCH /v1/brands` validates `country`'s casing at all, so an admin typing "de" instead of "DE" into the real "Add brand"/correction form would show the raw lowercase code on the real public car page instead of "Germany" — the same shape of data-entry-casing mistake the Generation `startYear`/`endYear` fix (also same day) closed for a different field. Fixed by uppercasing the code before resolution. 1 new `packages/utils` test. **Real gap found and fixed 2026-09-08**: `GET /v1/cars/:brandSlug/:modelSlug`'s `generations` include had no `orderBy` at all — verified live with a real, deliberately-out-of-chronological-order insertion (a 2025 generation created before a 1990 one): they came back in plain insertion order, not chronological order. A real editor backfilling a car's older generations after its current one already exists — a natural way to actually use the real "Add generation" admin feature built earlier this session — would scramble the real public car page's display order. Fixed with `orderBy: {startYear: "asc"}`; verified Postgres's real null-handling too (a generation with no recorded `startYear` sorts last, not first, confirmed live with a real mixed-null dataset, not assumed). Also closed a separate test-coverage gap found while fixing this: `GET /v1/cars/:brandSlug/:modelSlug` had zero direct integration tests at all (only the sibling `PATCH` was tested) — added a 404 test and the ordering test itself (3 real generations — older/current/newer plus a null-year one — inserted deliberately out of order, response confirmed chronological). 2 new `apps/api` integration tests. **Same pass, extended to the nested trims/engines/batteries**: none of the three had any ordering either — lower urgency than generations (no equally dramatic real-world scenario demonstrates it, and Postgres tends to preserve insertion order in practice for this shape of query), but the same underlying non-determinism per SQL semantics. Neither `Trim`/`Engine`/`Battery` has a `createdAt` field (no schema migration for this) — ordered by `id` instead, a real cuid that's lexicographically time-ordered by construction, verified live (created two real trims 50ms apart, confirmed `id` sorts them in creation order, not alphabetically) before relying on it. 1 new `apps/api` test (two real trims named so alphabetical order would reverse them — confirms creation order, not name order, is what's actually happening). **Same pass, third finding, extended past the query nesting boundary**: the car page's "Related stories" section (`relatedStories`, sourced via a separate `entityRelation`→`story` lookup, not nested inside the same `include`) had no ordering either — the exact same gap shape, just one query away. Only 1 real related story exists for any real car today (entity extraction is real but still young), so this hasn't visibly scrambled a real page yet, but every OTHER real story list in this app (the homepage feed, topic pages, `/admin/stories`) already orders by `lastUpdatedAt: desc`, and this is the same kind of list. Fixed with the same ordering. 1 new `apps/api` test (two real fixture Stories with deliberately-reversed `lastUpdatedAt` values, linked via real `EntityRelation` rows, confirm the newer one comes first). |
| Automated tests (spec §59) | **283 real unit/integration tests** (`npm test`, needs `DATABASE_URL` **and `REDIS_URL`** set — see Quickstart): `apps/api` (141, integration + the `rate-limit` unit tests + the correlation-id/security-header/query-parser tests + the fallthrough-error-handling tests + the `SourceScoreEvent` write/read tests + the `POST /v1/sources` create-source tests + the `POST`/`PATCH /v1/brands`/`/v1/cars` create+correct tests + the `POST`/`PATCH /v1/cars/.../generations`/`/v1/generations/.../trims` create+correct tests + the `POST`/`PATCH /v1/trims/.../engines`/`/v1/trims/.../batteries` create+correct tests + the negative-number validation-bound tests + the Generation `endYear`-before-`startYear` rejection tests (on both create and a merged-with-existing-row correction) + the slug-format rejection tests (Brand/CarModel/Generation/Trim) + the malformed-`url`/`feedUrl` rejection test + the `GET /v1/cars/:brandSlug/:modelSlug` 404/generation-chronological-ordering/trim-creation-ordering/relatedStories-newest-first-ordering tests + the fact-creation bogus-`marketId`-stays-404-and-doesn't-crash-the-process test + the malformed-session-cookie-is-401-not-500 tests + the `GET /ready` DB-unreachable-returns-503 test + the `GET /v1/stories` offset-pagination tests (incl. a real regression guard that a stack trace/filesystem path never appears in a response body again) + the `GET /v1/alerts` and `GET /v1/audit-log` offset-pagination tests (found live against a dev DB with 1,500+ real AuditLog rows already hidden past the old hardcoded `take:100`) + the `GET /v1/search-queries` offset-pagination tests (found live against 700+ real rows hidden past the old hardcoded `take:50`) + the `POST /v1/analytics/events` public-access/validation/rate-limit tests (incl. a real two-distinct-forwarded-IPs proof that the new limiter doesn't collapse every real visitor onto one bucket) + the `POST`/`PATCH /v1/redirects` self-referential/two-hop-cycle rejection tests + a real live-session-revoked-on-suspension test (a second real login, real access proven before suspension, real 401/403 after, same untouched session cookie throughout) — now including `/v1/search`'s brand-name fix + `/v1/topics` + `PATCH /v1/alerts/:id/resolve` + `GET /v1/audit-log` + the user-management endpoint tests + the fact-correction endpoint tests (incl. the new `GET /v1/markets` + marketId-on-create tests) + the redirect-management endpoint tests + the analytics/search-query observability endpoint tests + the `GET /v1/stories` real-byline test + the `GET /v1/sources` crawlInterval/robotsStatus exposure test) + `apps/worker` (56, including `ingestSource()`'s entity-extraction path, the inactive-source guard, the BullMQ scheduler tests against a real local Redis incl. the retry/backoff config, the exhausted-retries `SystemAlert` tests, the `parseFeed()`/`ingestSource()` real-byline-extraction tests, the `isPathAllowed()`/robots.txt-compliance tests (incl. the real wildcard/`$`-anchor pattern tests and the malformed-feedUrl-fails-open test), the `parseDate()` malformed/absent-pubDate-returns-null tests, the feed-fetch-failure real-WARNING-alert-and-dedup tests, the `normalizeUrl()` real-vs-unrelated "ref"-param tests, the real-importanceScore-boost-on-multi-source-clustering test, the new fetch-timeout tests, and the exhausted-retries-CRITICAL-alert-dedup test) + `packages/editorial` (57, including the new `entity-extractor` and `computeImportanceScore()` tests, plus the real fatal-headline safety-recalls and layoff/job-cut market-business classification tests, plus the new `evaluateQualityGate()` hard-floor-genuinely-overrides-a-would-be-publish tests, plus the new `summarizeKnowledgeDelta()` real-zero-numericDelta tests, plus the new `groupByInformationOrigin()` null-vs-omitted-originUrl test) + `packages/ai` (10, integration — incl. the new `recordExecution()` full-field-write/failed-execution/aggregation-visibility tests + the floating-point-boundary `assertWithinBudget()` test + the new `handleBudgetExceeded()` job-PENDING/SystemAlert test) + `packages/seo` (11, incl. the new `buildBreadcrumbJsonLd()` tests + the new `safeJsonLdString()` tests — see the stored-XSS finding below) + `packages/utils` (8, incl. the new `formatCountryName()` tests + the lowercase-country-code case-insensitivity test). **Plus a real E2E tier**: `npm run e2e` — 66 Playwright tests (65 assertions + 1 shared login step, see below) driving an actual Chromium against actual `apps/api`+`apps/web` dev servers (homepage, topic pages, car pages incl. 404s, search, admin login incl. wrong-password, the real seeded admin dashboard/stories/alerts/audit-log/users/cars/redirects/analytics pages with session carrying across all of them — including a real alert-resolve flow through the actual "Resolve" button, a real mutation showing up on the real audit-log page, a real create-user-then-suspend flow through `/admin/users`, a real add-fact-then-correct flow through `/admin/cars` (now also selecting a real market), a real add-brand-then-add-car-model flow through `/admin/cars`, a real add-generation-then-add-trim-then-add-engine flow through `/admin/cars`, a real add-redirect-then-correct flow through `/admin/redirects`, a real zero-result search shown flagged on `/admin/analytics`, a real deactivate-then-correct-trust-score flow through `/admin`'s Sources dashboard, a real add-source-then-duplicate-URL-error flow, and a real end-to-end logout, new 2026-09-07 — sitemap/robots content, the real redirect, all 6 `/about/*` credibility pages, the real `AnalyticsEvent` pageview write, and real automated accessibility checks via `@axe-core/playwright` on 20 real pages including the 8 session-gated admin ones: **zero WCAG violations found**, a real result of this project's zero-client-JS/plain-semantic-HTML architecture, not assumed). **Real test-coverage gap found and fixed 2026-09-07 (same day, later pass)**: this a11y suite only ever covered 3 of the 6 real `/about/*` pages — `/about/corrections`, `/about/editorial-policy`, `/about/how-we-use-ai` were never run through axe-core at all despite being real, indexable, sitemap-listed pages with their own real content. Found by enumerating every real `page.tsx` in the app and diffing against the test's own page list, not by re-checking pages already covered. Added all 3 — all pass with zero real violations, same as every other page, but the coverage gap itself (nothing would have caught a future regression on these 3 pages) is what this fixes. Wired into CI. **Real gap found and fixed 2026-09-07 (E2E auth architecture)**: this suite used to log in fresh — a real browser-driven form submission — at the start of nearly every admin test, plus once per admin page in the accessibility suite's `beforeEach`. That was already sitting exactly at `apps/api`'s real login rate limit (10 attempts/15min); adding this tick's `/admin/users` tests pushed it over for real (caught by actually running the full suite: login started silently 429ing and every downstream assertion failed with a misleading "still on /admin/login" error). Fixed with Playwright's own recommended pattern — a `setup` project (`e2e/auth.setup.ts`) logs in for real exactly once per run and saves the session via `storageState`; every other admin test loads that file via `test.use({ storageState })` instead of resubmitting the form. The real login flow itself is still fully exercised, just in one authoritative place instead of a dozen — the same "shared login instead of one per test" fix already applied to `apps/api`'s own integration tests earlier the same day. |
| Source independence (`packages/editorial`, extension §52-54) | **Wired into real production 2026-09-09** (user's explicit request). Was unit-tested since 2026-09-08 but genuinely unwireable as designed: `groupByInformationOrigin()` groups by `originUrl`, "the primary source... this article cites" — a field that does not exist anywhere on the real `SourceArticle` schema and nothing detects; calling it as-is would have been theater (every article falling back to its own id, making the count mathematically equal to `articles.length`, zero real signal) — checked this directly before wiring anything, not assumed. Real fix: `apps/worker/src/write-article.ts`'s new `countIndependentOriginsByEmbedding()` reuses `SourceArticle.embedding` (already computed at ingest time for duplicate detection, zero extra cost) — sources whose headlines are near-identical (cosine similarity > 0.92, well above the ~0.78 seen for two independently-written headlines about the same real event) are treated as one origin, approximating "these outlets just reworded the same wire copy" without needing real citation-extraction. Feeds directly into the real Fact Checker's `sourceScore` judgment (`fact-check.ts`) as explicit context. **Live-verified**: a real single-source story's fact-check concerns explicitly cited "Only one independent source provided" in its own reasoning, confirming the signal is actually read and used, not just passed through unread. `/about/editorial-policy` updated to describe this honestly as an approximate, wording-similarity-based signal, not a literal citation trace. |
| Unit localization engine (`packages/utils`, spec §93-94) | **Done, unit-tested** — `formatDistanceKm`/`formatPowerKw` pick km/miles or kW/hp by `UnitSystem`, verified against docs' own exact worked example (500 km → "311 miles") and the real seeded BMW M3 Competition figures (375 kW ↔ 503 hp) round-tripping correctly. Not yet wired into a page — no page has a real locale/market context to format for yet (only English exists, car page currently shows both hp and kW plainly, which is honest and not wrong, just not locale-switched). |
| hreflang/canonical (`packages/seo`, spec §22/§30) | **Done, tested live on both pages that exist** — `buildHreflangAlternates()`/`buildLocaleUrl()` encode the real URL scheme (English at root, Spanish under `/es/`, never a subdomain) in one place; wired into both the homepage and the car page's metadata, verified via curl on each that real `<link rel="canonical">` + 3 `<link rel="alternate" hreflang>` tags render. The `/es/` edition itself doesn't exist yet — the alternate link is still correct to emit now. |
| Editorial ranking (`packages/editorial`, spec §95) | **Done, unit-tested, and wired into the real homepage** — `computeRankingScore()` now orders `apps/web`'s "Latest" feed (previously just `lastUpdatedAt`). Verified live with a real DB mutation: bumping one 15th-ranked (older) story's `importanceScore` to 100 correctly moved it to #1 despite its age — proves the reordering is real computation, not coincidence with the old sort. **Real gap found and fixed 2026-09-07**: that same `importanceScore` — `WEIGHTS.importance: 0.35`, the single largest weight of the five `computeRankingScore()` inputs — had sat at its schema default (30) for every real Story ever ingested; nothing anywhere ever set it to anything else outside that one manual test mutation. Confirmed live before fixing: 149 real Stories in this dev DB, exactly 1 with 2+ real distinct sources covering it, and that one still stuck at 30 like every single-source story — the ranking algorithm's biggest input was a dead constant. Fixed with a new `computeImportanceScore(sourceCount)` (`packages/editorial`, real unit tests): a real, honest, currently-computable signal — deliberately based on plain distinct-source count, not the not-yet-real origin-deduplicated count `source-independence.ts` will eventually provide once an AI research stage can detect an article's `originUrl` (using the un-deduplicated count here would risk crediting 3 outlets that turn out to all be reprinting one press release as more "important" than they actually are — an honest limitation, not hidden). `ingestSource()` now recomputes it on every item from the real, current `StorySource` count — a no-op for a brand-new single-source story, a real boost the moment a second outlet's coverage clusters in. A new `apps/worker/src/backfill-importance.ts` (`npm run backfill:importance`, idempotent, same shape as the topic/author backfills) retroactively corrects existing Stories; real run: exactly the 1 real multi-source Story got corrected (30 → 55), 148 single-source Stories correctly left untouched. |
| `KnowledgeDelta` computation (`packages/editorial`, extension §8) | **Done, unit-tested** — `computeFactDeltas()` diffs old vs. new Fact values (numeric delta when both parse as numbers, e.g. the docs' own Model Y range example; non-numeric changes like a status flip still count as real change with `numericDelta: null`); `summarizeKnowledgeDelta()` rolls that into the single number `decideComposition()` consumes. **Real test-coverage gap found and fixed 2026-09-08**: `summarizeKnowledgeDelta()`'s own `Math.abs(d.numericDelta) || 1` fallback exists specifically for a third case its own comment doesn't call out — a changed fact whose two string values parse to the identical number (e.g. `"155"` → `"155.0"`, a real, plausible scenario when a later crawl re-reports the same fact with different formatting): `changed: true` but a real, non-null `numericDelta` of exactly `0`. Nothing tested this distinct-from-both-neighbors path before (unlike the string-equal "no change" case, which short-circuits before numeric parsing, and the non-numeric-change case, whose `numericDelta` is `null` not `0`) — a future edit swapping `|| 1` for `?? 1` (which would NOT catch a falsy-but-defined `0`) could have silently regressed this to summing as `0`. Added 2 new tests closing the gap. **Still has zero real caller as of 2026-09-09** — checked directly (no real code outside its own tests imports `computeFactDeltas`/`summarizeKnowledgeDelta`) while wiring the row below (`ContentCompositionEngine`) into production the same day. Genuinely a separate gap, not the same one: this module is shaped for diffing `CarModel`/`Fact` attribute values, and that pipeline has no automated update flow at all yet — every real `Fact` is manually seeded (see the Automotive Knowledge Base row below), so there's no real trigger that would ever call this. Wiring it for real means building that trigger (an admin or AI flow that detects a changed car spec), not just adding a call site — a separate, currently-unscoped feature, not a quick follow-up to the row below. |
| `ContentCompositionEngine` (`packages/editorial`, spec extension §6, cannibalization prevention) | **Wired into real production 2026-09-09** (user's explicit request), closing a real, structural gap: `decideComposition()` was unit-tested since 2026-09-08 but had zero real caller — not just an oversight, `runWriteArticleBatch()`'s only query (`articles: { none: { locale: "en" } }`) excluded every Story that already had an article, so no candidate for UPDATE/MERGE/REJECT could ever exist; every real Story only ever went through CREATE. Added a real second path in `write-article.ts`: Stories with an existing EN article that gathered genuinely new `SourceArticle` coverage since the article's own `updatedAt` (not `Story.lastUpdatedAt`/`status` — both get set on the first-ever update and never reset, so neither means "new since last write") now go through `decideComposition()` and, on UPDATE, get a real appended block containing ONLY the new material — still passed through the real Fact Checker/Quality Gate (a "reject" verdict there means don't touch the live article) — plus a real `ArticleRevision` row (`packages/database`, existed since the schema scaffold, never written to before this either). **Live-verified end-to-end**: manually staged a real new source article for a real published Story ("Port Authority, CALSTART..."), confirmed a real, accurate, non-repeating update paragraph was appended, a real revision was recorded, `updatedAt` bumped (feeding `sitemap.xml`'s real `lastmod`) — then reverted the synthetic test data before it could mislead a real reader. `computeFactDeltas()`/`KnowledgeDelta` (below) is intentionally NOT used for this — it's shaped for `CarModel`/`Fact` attribute diffs, not "N new pieces of RSS coverage arrived"; the `knowledgeDelta` fed to `decideComposition()` here is a new-source-count proxy specific to this pipeline. |rpose, UPDATE when one does and there's new knowledgeDelta, REJECT when there isn't, MERGE when duplicate-purpose drift already exists. Not yet wired into an actual AIJob (no article-writing pipeline exists yet). **Documentation gap checked and fixed 2026-09-08**: `docs/editorial-system.md`'s own outcome table names 5 real outcomes, but this function only ever returns 4 — `SPLIT` ("one existing article has grown to cover two distinct purposes") is never produced, and nothing explained why. Confirmed this is correctly deferred, not an oversight: detecting it requires understanding an existing article's actual current CONTENT well enough to tell it now straddles two purposes — this function only ever sees an already-assigned single `contentPurpose` per article, which structurally can't express that. Real content analysis, the same AI-blocked category as the AIProvider/Quality Gate rows above. Added a comment explaining this; kept `SPLIT` in the public type since it's a real future outcome callers should already be prepared for. |
| AI Quality Gate (`packages/editorial`, spec §36) | **Done, unit-tested** — `evaluateQualityGate()`: configurable `publishAt`/`reviewAt` thresholds on a weighted average of the 6 scores (factual/source weighted highest), plus a hard floor on factualScore/sourceScore that blocks publish regardless of how high other scores are. Not yet wired to a real quality-scoring AI stage (doesn't exist yet). **Real test-coverage gap found and fixed 2026-09-08**: this file's own comment states the hard floor's whole purpose is stopping "a beautifully-written article built on shaky facts... from averaging out to a publish" — but every existing test for it used scores whose weighted average was *already* below `publishAt` before the hard floor even applied, so they'd have passed identically with the hard-floor branches deleted outright, never actually proving the documented behavior. Added 2 new tests that construct the real case (every score at 100 except one hard-floor field at 20 — an average that clears `publishAt` on its own) confirming the floor genuinely downgrades a would-be "publish" to "review", plus 1 confirming a low hard-floor score still correctly "reject"s when the overall average is also genuinely poor (not unconditionally capped at "review"). Also simplified the factualScore branch's own verdict expression — a 3-way ternary with two branches yielding an identical result (redundant, not a behavioral bug, but read as if crossing `publishAt` did something distinct when the real rule is simpler) — to the same direct form the sourceScore branch already used. Not yet wired to any real caller (isolated `packages/editorial` scaffolding, same as the rest of this row). |
| `sitemap.xml` / `robots.txt` (spec §29-31) | **Done, tested live + locked in by E2E** — real Next.js `MetadataRoute` routes, sitemap lists the homepage + every real seeded car page (`GET /v1/cars`) + every real topic page (`GET /v1/topics`, added earlier after finding topic pages were linked from the homepage but missing from the sitemap — not a static stub). **Real gap found and fixed 2026-09-07 (same day)**: all 6 real, indexable `/about/*` pages (each already emitting a real canonical/hreflang — see the row above) were missing from the sitemap too, despite this file's own comment claiming to enumerate "actual indexable pages" — `/search` correctly stays excluded since it deliberately sets `robots: { index: false }`. Fixed by adding all 6 `/about/*` URLs, `/about/sources` at `weekly` changefreq (a live DB-backed source list) and the other 5 at `monthly` (static policy text). Verified live via curl against a real running dev server; full regression sweep re-run clean (155/155 unit+integration, 53/53 E2E — extended the existing sitemap E2E assertion with 3 new `/about/*` URL checks rather than adding a new test). | **Real gap found and fixed 2026-09-08**: parsed the real live sitemap.xml output as real XML and confirmed it's well-formed with 11 sensible entries (correct priorities/changefreqs), but no entry anywhere set `lastModified` -- real timestamp data existed for both car pages and topic pages that could back it, so this was a genuine, low-risk SEO gap, not a bug -- fixed the same tick it was found rather than just documented, once the real scope (widening `GET /v1/cars`/`GET /v1/topics`'s response shape) was fully understood. `GET /v1/cars` now returns each car's most recent real `Fact.createdAt` (`null` for a car with no Fact yet, a real valid state); `GET /v1/topics` was widened from a bare `topics: string[]` to `{ slug, lastModified }[]` (a real, small breaking shape change -- updated both real callers, `apps/web/src/app/page.tsx` and `sitemap.ts`, plus the one existing test asserting the old shape) carrying each topic's most recent real `Story.lastUpdatedAt`. `lib/api.ts`'s matching types updated; `sitemap.ts` substitutes `undefined` for a `null` `lastModified` so Next's own `MetadataRoute` type omits the field entirely rather than emitting an empty `<lastmod>`. Verified live end-to-end, not just unit-tested: booted real `apps/api`+`apps/web` dev servers, curled the real `GET /v1/cars`/`GET /v1/topics` endpoints directly (real timestamps present and sensible), then parsed the real live sitemap.xml as real XML again and confirmed the car page and all 3 topic pages now show a real `<lastmod>` matching the API's own values, while the homepage and all 6 `/about/*` pages (which have no real timestamp source wired in, correctly out of scope) show none at all -- not an empty tag, confirming the `undefined`-substitution works as intended. Added 1 new `apps/api` test (a topic's `lastModified` matches its most recently updated real Story, queried independently via Prisma). Full regression sweep clean: 282/282 unit+integration (1 new), 66/66 E2E (unchanged -- no new user-facing behavior for a crawler-only signal to assert).
| Organization/WebSite JSON-LD (spec §29) | **Done, tested live** on every page (root layout). **Real gap found and fixed 2026-09-07 (BreadcrumbList)**: the root layout's own comment bundled BreadcrumbList together with Article/NewsArticle/ImageObject as all waiting on "those page types" existing — but unlike the other three, a breadcrumb only needs real pages to link to, not a real article or a real photo, and the car page/topic page/6 `/about/*` pages all already have real, non-fabricated navigational hierarchy (no invented `/cars` or `/topics` index page — breadcrumbs stop at the real routes that exist). Added `packages/seo`'s `buildBreadcrumbJsonLd()` (new file, 2 real unit tests) and wired it into all 8 of those pages. Article/NewsArticle/ImageObject correctly remain deferred — no real article or real photo exists yet to describe (car-page photos are a separate, still-deferred TODO — see the car-page row). Verified live via curl on the car page, a topic page, and all 6 About pages: each renders the correct real `BreadcrumbList` with 1-indexed positions and real URLs. Full regression sweep clean: 157/157 unit+integration (2 new), 53/53 E2E (unchanged, no new assertion added — a real, deliberate follow-up, matching the precedent already set for the hreflang fixes on the same pages). **Severe real stored-XSS gap found and fixed 2026-09-08**: every one of these 10 real JSON-LD `<script type="application/ld+json">` injections (this row's Organization/WebSite blocks plus all 8 breadcrumb pages above) used `dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}` with zero escaping — verified live (`node -e`) that `JSON.stringify()` does not escape `<`, so a value containing the literal string `</script>` closes the real script tag early, letting anything after it run as real HTML/script. The car page's own breadcrumb interpolates `carModel.brand.name`/`carModel.name` directly, and `POST /v1/brands`/`POST /v1/cars` validate those fields with only `z.string().min(1)` — no character restriction at all — making this a genuine, currently-exploitable stored XSS, not a contrived one: any authenticated editor (or a compromised/mistaken admin account) entering a brand/model name containing `</script><script>...</script>` would have that script execute in every real visitor's browser on that public car page. Fixed with `packages/seo`'s new `safeJsonLdString()`, which escapes every literal `<` to its unicode escape `\u003c` (still valid, spec-legal JSON, so every real JSON-LD consumer parses it identically) before injection; all 10 usages across `apps/web/src/app/layout.tsx` and the 8 breadcrumb pages switched over, confirmed via grep that zero raw `JSON.stringify`-into-`dangerouslySetInnerHTML` usages remain anywhere in `apps/web`. Verified end-to-end against the real, live, exact vulnerable path, not just unit-tested: booted real `apps/api`+`apps/web` dev servers, logged in as the real seeded admin, created a real fixture `Brand` with name `"Foo</script><script>window.__xss_proof=1</script>"` (accepted, 201 — confirming the zero-restriction validation), created a real fixture `CarModel` under it, fetched the real public car page, and confirmed the raw HTML response now shows the payload correctly escaped inside the JSON-LD block (`Foo\u003c/script>\u003cscript>window.__xss_proof=1\u003c/script>` — never a literal `<` that could break the tag), while the real script tag itself closes correctly right after. Fixture rows deleted and both dev servers killed immediately after verification. Full regression sweep clean: 281/281 unit+integration (2 new, in `packages/seo`), 66/66 E2E (unchanged — a real, deliberate follow-up matching this project's precedent for security-hardening fixes with no new user-facing behavior to assert). |
| Custom 404 page (`apps/web`) | **Done, tested live** — themed instead of Next.js's bare default |
| Custom error boundary (`apps/web/src/app/error.tsx`) | **Real gap found and fixed 2026-09-07** — `apps/web` had a themed custom 404 (above) but no matching `error.tsx`, so a real Server Component failure (e.g. `apps/api` briefly unreachable — a real operational scenario, verified live by actually killing `apps/api` and hitting the homepage against a real `next build && next start`, not contrived) fell all the way through to Next's own built-in `global-error` fallback. Confirmed by reading the actual compiled source (`node_modules/next/dist/client/components/builtin/global-error.js`): reasonably designed, no leaked stack trace (only a `digest`) — but it replaces the entire `<html>` document per Next's own bundled docs ("Global error UI... is replacing the root layout"), discarding this site's own header/nav/JSON-LD. Fixed with a route-level `error.tsx` (must be a Client Component — the one deliberate exception to this project's near-zero-client-JS pages) that catches the same failure one level lower, staying inside the root layout. Hit a real version-specific detail while writing it: this project's installed Next.js has real API differences from training data (`apps/web/AGENTS.md`'s own standing warning) — checked the bundled docs before finalizing and found the documented callback prop is named `retry`, not the more commonly-known `reset` (confirmed both are actually provided by the real runtime by reading `error-boundary.js` directly, so neither name would have been a functional bug — used `retry` to match the current documented convention). Verified the fix took effect via a clean before/after comparison of the real production RSC payload: pre-fix explicitly referenced `__next_builtin__global-error.js`; post-fix contains no such reference and instead shows the custom boundary's own compiled chunk alongside the layout's real header markup — full pixel-level browser confirmation wasn't available this session (a known, pre-existing `claude-in-chrome` tooling limitation in this environment, re-confirmed and not re-litigated further), so this is verified at the payload/structural level, not a screenshot. **Re-verified 2026-09-07 (same day, one more pass) — found the original verification's own methodology had a real blind spot, and the fix does not currently hold.** The original check confirmed the custom boundary's compiled chunk was *referenced* in the real production RSC payload — but never confirmed the visible rendered TEXT ("Something went wrong") actually appears anywhere in the response. Re-tested the exact same way (real `next build && next start`, apps/api killed) and did a direct byte-for-byte comparison against a real, healthy page's response: the healthy page's real content ("3 Series") appears directly in the raw HTML; the error case's raw HTML contains neither error.tsx's nor a newly-added `global-error.tsx`'s text anywhere, only a minimal `<html id="__next_error__">` shell around a React Flight bootstrap script — a chunk *reference* being present is not the same as that chunk's content actually rendering. Isolated the cause with a minimal, dependency-free diagnostic page (`throw new Error(...)` directly, no fetch, no apps/api at all) in both `next start` and `next dev`: route-level `error.tsx` caught neither. Per this exact Next.js version's own bundled reference doc's "Version History" table: the `retry` prop only became stable in v16.3.0; this project runs v16.3.4, four patch releases later — plausibly a real, version-specific regression in a very recently stabilized area of the framework, not fixable from application code. Added `global-error.tsx` (the next-best documented mechanism) alongside error.tsx, but its own effect could not be conclusively verified either: Next's Flight protocol appears to resolve which boundary actually renders during client-side hydration for this failure mode, not synchronously in the raw SSR response, and the one tool available for real browser JS execution could not reach `localhost` at all in this environment — confirmed as a genuine environment limitation, not specific to this investigation, since it also failed on a known-healthy baseline page, not just the error case. Both files kept in place (genuinely spec-compliant, cost nothing, `error.tsx` takes priority the moment a future Next.js patch fixes this) but "does a real user see a branded message instead of a blank/broken page during a real outage" is recorded here as a real, currently-open question, not a confirmed fact — full regression sweep otherwise clean: 250/250 unit tests unchanged, full E2E 66/66 unchanged (this investigation touched only error.tsx/global-error.tsx, both outside the E2E suite's own coverage). | **Second real live reproduction, 2026-09-08, via a different trigger**: while separately live-testing whether `apps/web/src/lib/api.ts`'s own `AbortSignal.timeout(10_000)` fetch-timeout fix (added 2026-09-07, documented in that file, never actually tested against a genuinely hung upstream before now) really bounds a stalled page load, booted a real Node HTTP server that accepts a connection but never responds, pointed both a real `next dev` and a real `next build && next start` at it as `API_INTERNAL_URL`, and curled the homepage and the car page. Confirms two things: (1) the fetch-timeout fix genuinely works -- neither page hangs forever, both resolve with a `500` -- but the real worst-case latency is **~20 seconds, not ~10**, reproduced identically in dev and production and across both a multi-fetch page (homepage) and a single-fetch page (car page), root cause fully pinned down (an earlier draft of this same finding guessed "a Next.js App Router retry," before capturing the fake upstream's own request-timestamp log disproved it): `apps/web/src/proxy.ts` runs on every matched request BEFORE the page even renders and makes its OWN `/v1/redirects/lookup` fetch with its own independent 10s timeout, swallowed by its own try/catch ("a redirect lookup failure must never break normal navigation") -- only THEN does the actual page's own data fetch begin, paying `lib/api.ts`'s same 10s timeout a second time. Two independent, architecturally sequential 10s timeouts, not a framework retry -- now documented directly in `lib/api.ts`'s own comment as a real, verified characteristic, not assumed from the constant's name. (2) Independently reproduces this exact row's own still-open question via a different failure trigger (a hung fetch timing out, not `apps/api` killed outright): the raw HTML response here also showed only a minimal `<html id="__next_error__">` shell with a `next-error` meta tag and an opaque `digest` (confirmed safe -- no leaked stack trace or filesystem path either way), matching the prior investigation's own finding that `error.tsx`'s visible text could not be confirmed present in the raw SSR response. Does not resolve the open question (still blocked on the same real browser-JS-execution environment limitation), but confirms it reproduces via at least two independent, different real failure modes, not one investigation's fluke. No regression sweep needed -- `lib/api.ts` comment-only change, `apps/web` typecheck re-run clean; test/e2e counts unaffected (281 unit/integration unchanged, no e2e coverage of this failure path either way).
| Fact extraction / research / editorial synthesis / quality gate | **Quality gate half done and live-verified 2026-09-09** (user's explicit request): `packages/editorial/src/quality-gate.ts`'s `evaluateQualityGate()` existed since 2026-09-08 fully unit-tested but with zero real caller — nothing computed the 6 scores it needs, so `AUTO_MODERATION` stayed permanently off. Added `apps/worker/src/fact-check.ts`: a real second Claude Haiku call per article, re-reading the draft against its own source excerpts and scoring it honestly (factualScore weighted 2x, matching the gate's own "shaky facts must not average out to a publish" design). Wired into `write-article.ts`: a `review`/`reject` verdict overrides `AUTO_PUBLISH`, a `publish` verdict still respects it as a separate gate. `AUTO_MODERATION=true` live on production. Real cost: ~$0.0034/article now (was ~$0.0018), confirmed from real `AIExecution` rows. **Caught a real problem on its very first live run**: a single-source story ("Dodge Durango 2027...") where the Writer added unverifiable interpretive framing ("pattern of gradual enhancements... incremental updates have become characteristic") — correctly held as `DRAFT` (sourceScore 25, hard-floor review) instead of auto-publishing, confirmed via the real stored scores on that Article row. Independent research/fact-extraction beyond checking the draft against its own already-ingested sources (the original, larger ambition — an AI stage that goes and verifies a claim against outside evidence) is still not built — this closes the gate/verification half, not the research half. |
| Real AI provider (Anthropic/OpenAI) behind `AIProvider` | **Stale row, corrected 2026-09-09** — this said "not implemented" from the original scaffold and was never updated once it was. Both are real and live: `AnthropicProvider` (`claude-haiku-4-5`, see row 18 above for the implementation) has been writing every published Article since 2026-09-08, and a real OpenAI `gpt-image-1-mini` call (`apps/worker/src/generate-image.ts`) generates hero images as the fallback when free-stock search finds nothing. Found stale while doing a full pass against the user's "finish everything planned" request — this row directly contradicted row 19 below, which already documents the real thing in detail. |
`/about` section: index + how-we-use-ai + corrections + sources + editorial-policy + contact (spec §67-69, §35) | **Done, tested live + locked in by E2E** (all 6 pages, plus `/about/sources` lists the real seeded sources). Honest, current-state content, cross-linked for real. `/about/contact` deliberately has no invented email/company (none exists yet — a real TODO, not a fabricated dead end). **Real gap found and fixed 2026-09-07 (hreflang/canonical)**: the homepage, car page, and topic page already emit real `buildHreflangAlternates()`/`buildLocaleUrl()` metadata (spec §22/§30, see the row above), but all 6 `/about/*` pages had never gotten the same treatment — confirmed by grepping every page under `/about/*` plus `/search` for the two function names before touching anything (`/search` correctly excluded, since it already sets `robots: { index: false }`). Fixed by adding the same `alternates: { canonical, languages }` block, scoped to each page's own real path, to all 6 pages' static `metadata` exports. Verified live against a real production `next build && next start`: curl on `/about`, `/about/sources`, and `/about/contact` all show a correct page-specific `<link rel="canonical">` plus the 3 correct `hreflang` alternates. Full regression sweep re-run clean after the fix: 155/155 unit+integration tests, 53/53 Playwright E2E tests. Remaining on this row: Privacy, Terms, Cookie Policy — these need real legal review to be meaningful, not authored as generic boilerplate here. |
| Admin login + dashboard + stories queue + alerts + audit log + user management + car fact correction + redirect management + analytics/search observability (`/admin/login`, `/admin`, `/admin/stories`, `/admin/alerts`, `/admin/audit-log`, `/admin/users`, `/admin/cars`, `/admin/redirects`, `/admin/analytics`, spec §37-38, §40, §24/§90-91, §29-31, §32/§49) | **Done, tested end-to-end live + locked in by E2E** — first real interactive (Client Component) piece of `apps/web`: a login form posts to a same-origin proxy route (`/api/admin/login`, forwards to the real `apps/api` and relays its `Set-Cookie`, so no CORS setup needed and it matches production's same-domain nginx topology). All eight admin pages share a real server-side session check (`requireAdminSession()`, forwards the cookie to `apps/api`'s real `/v1/auth/me`). Verified via curl and Playwright E2E: no session → redirect to `/admin/login`; wrong password → real error text, no navigation; real login → `/admin` shows the logged-in user + real sources with trust scores, `/admin/stories` shows all real ingested Stories, `/admin/alerts` shows the real alert feed with a "Resolve" button per unresolved alert, `/admin/audit-log` shows the real audit trail, `/admin/users` shows real accounts with real create/suspend/activate controls, `/admin/cars` shows real per-car Facts with real correct/add controls, `/admin/redirects` shows every real redirect with real correct/add controls, `/admin/analytics` shows real top pages and real recent searches — session cookie confirmed carrying across all eight. No editorial-review action buttons (Approve/Reject/Regenerate/...) yet — nothing exists for them to act on. **Real security gap found and fixed 2026-09-07**: the session cookie had no `Secure` flag at all — it would have been sent over plain HTTP even in production. Fixed with `secure: env.NODE_ENV === "production"` (unconditional `secure: true` would break login on this project's own `http://localhost` dev/E2E setup, so it's conditional, not blanket). Locked in with an integration test asserting `HttpOnly`+`SameSite=Lax` always and `Secure` only in production — verified the fix has real effect by forcing `NODE_ENV=production` locally and confirming `supertest`'s plain-HTTP agent correctly refuses to send the now-`Secure` cookie on the follow-up request, same as a real browser would. **Real gap found and fixed 2026-09-07 (audit log)**: `PermissionKey.VIEW_AUDIT_LOG` already existed and every admin already had it granted, and two mutating endpoints already wrote real `AuditLog` rows — but nothing could ever read the table back, so the entire audit trail was permanently write-only. Fixed with `GET /v1/audit-log` (`VIEW_AUDIT_LOG`-gated, filterable by `entityType`/`entityId`) and a new read-only `/admin/audit-log` page (a `<details>` per entry showing the real before/after JSON diff). Caught and fixed a second real bug while wiring the display: `AuditLog.actorLabel`'s own schema comment calls it "a human-readable fallback," but every write site set it to the raw `User.id` — fixed with a shared `auditActorLabel()` helper that resolves the actor's real email, falling back to the id only if the user record no longer exists. Verified live end-to-end: a real alert resolve produces a real audit row showing the actual admin email, visible on the real page. **Real gap found and fixed 2026-09-07 (user management)**: `PermissionKey.MANAGE_USERS` already existed and every admin already had it granted, but no endpoint ever checked it — `seed.ts`'s single hardcoded `admin@dev.local` was the only User row that could ever exist. Fixed with `GET /v1/users`, `GET /v1/roles`, `POST /v1/users` (real bcrypt-hashed password, real `UserRole` assignment, 409 on a duplicate email, 400 on an unknown role), and `PATCH /v1/users/:id/status` (activate/suspend), all `MANAGE_USERS`-gated and writing real `AuditLog` rows. A real lockout risk found and blocked before it could happen: the endpoint refuses to let an admin suspend their own account (the one admin action that couldn't be undone through the UI if it succeeded — every other action can be reversed via the UI itself), and the UI never renders a Suspend button on your own row either. Verified live end-to-end: created a real user through the real form, logged in as that user for real (proving the password hash actually works, not just that a row was written), suspended it through the real button, confirmed it in the DB. **Real gap found and fixed 2026-09-07 (car fact correction)**: `PermissionKey.UPDATE_CAR` already existed and admin already had it granted, but every `Fact` row (spec §90-91's time/market-aware system) could only ever come from `seed.ts` or the crawler — no human editor could correct a wrong spec/price or add a new one without a manual DB write. Fixed with `POST /v1/cars/:brandSlug/:modelSlug/facts` (creates a real `CONFIRMED`, max-confidence Fact — a human editor's direct assertion is a stronger claim than anything the crawler/AI stand-ins write today) and `PATCH /v1/facts/:id` (corrects an existing one), both `UPDATE_CAR`-gated and writing real `AuditLog` rows, plus `/admin/cars` reusing the same public `getCarModel()`/`getCarModelSlugs()` reads the public `/cars/*` pages already use (never permission-gated for reading) with real inline correction/add forms. Verified live end-to-end: added a real fact through the real form, corrected its value through the real per-fact form, confirmed both in the DB. **Real gap found and fixed 2026-09-07 (add a new Brand/CarModel)**: every Fact-correction endpoint above only ever worked on an *existing* `CarModel` — nothing anywhere let an editor create a brand-new `Brand` or `CarModel` at all, even though this platform's entire real Knowledge Graph is built around exactly those two entities; every one that exists in this database came from `seed.ts`. Same "manual DB write required" pattern already fixed today for Sources/Facts/Redirects/Users, just not yet applied to the two entities the whole platform is actually about. Fixed with `GET /v1/brands` (public read) + `POST /v1/brands` + `POST /v1/cars` (both `UPDATE_CAR`-gated — no dedicated `PermissionKey` for brand/model management, same "reuse an existing permission" precedent as `/v1/redirects`; 409 on a duplicate slug, 404 from `POST /v1/cars` if the referenced brand doesn't exist, both write real `AuditLog` rows) and two new forms on `/admin/cars` ("Add car model" with a real brand `<select>`, "Add brand"), through two new proxy routes (`/api/admin/cars`, `/api/admin/brands` — confirmed via a real build that both coexist cleanly alongside their pre-existing `[id]`/nested-dynamic siblings). Verified live end-to-end: added a real new brand and a real car model under it through the real forms, confirmed both via the real public `GET /v1/brands`/`GET /v1/cars`, confirmed the new car model's real public page (`/cars/{brand}/{model}`) renders. 8 new `apps/api` integration tests + 1 new Playwright E2E test. **Real gap found and fixed 2026-09-07 (correct a Brand/CarModel)**: every other admin entity built today (Source, Fact, Redirect, User) got both a create AND a correct path — Brand/CarModel only ever got create. Fixed with `PATCH /v1/brands/:id` (name/country) and `PATCH /v1/cars/:brandSlug/:modelSlug` (name — addressed by slug like the rest of this entity's routes, not a raw id, both for consistency and because Next.js's routing requires dynamic segments at the same tree position to share a name, which a raw-id sibling next to the existing `[brand]/[model]` routes would have violated — caught before writing the proxy route, not after a build failure), both `UPDATE_CAR`-gated and writing real `AuditLog` rows. `/admin/cars` gained an inline correction form per brand and per car model. Verified live end-to-end: corrected the real seeded BMW brand's name/country and the real seeded 3 Series model's name through the real forms, confirmed both via the real public API and the real admin page, then restored the real seeded values afterward. 6 new `apps/api` integration tests + 1 extended Playwright E2E assertion (continuing the same create-brand-then-create-model flow). **Real gap found and fixed 2026-09-07 (add a Generation/Trim)**: `POST /v1/cars` lets an editor create a brand-new `CarModel`, but the real `Generation → Trim → Engine/Battery` hierarchy — the structured data the public car page actually displays most prominently, above the flat Facts list — had no create path at all, and `/admin/cars` never even *displayed* it (only Facts). Scoped deliberately to Generation + Trim this pass (a Trim already renders meaningfully with zero engines, same "doesn't need every child row" reasoning as CarModel/Source) — Engine/Battery creation is a natural, separate follow-up. Fixed with `POST /v1/cars/:brandSlug/:modelSlug/generations` and `POST /v1/generations/:generationId/trims` (both `UPDATE_CAR`-gated, 409 on a duplicate slug, 404 if the parent doesn't exist, both write real `AuditLog` rows), `/admin/cars` now displaying each car's real generation/trim/engine tree read-only plus two new inline "Add generation"/"Add trim" forms, through two new proxy routes. Verified live end-to-end: added a real generation and a real trim under it through the real forms, confirmed both via the real public `GET /v1/cars/:brandSlug/:modelSlug`, confirmed the real public car page renders them. Cleaned up test fixtures in FK-safe child-first order (no `onDelete: Cascade` anywhere in this chain — verified in the schema, not assumed). 6 new `apps/api` integration tests + 1 new Playwright E2E test. **Real gap found and fixed 2026-09-07 (add an Engine/Battery — the last leaf of this hierarchy)**: the deliberate follow-up flagged in the previous entry, closed the same day. `POST /v1/trims/:trimId/engines` and `POST /v1/trims/:trimId/batteries` (both `UPDATE_CAR`-gated, 404 if the trim doesn't exist — neither model has a unique constraint, so no 409 case), `/admin/cars`'s generation/trim display extended to show each trim's real engines/batteries plus two new inline forms per trim. Verified live end-to-end: added a real engine and a real battery to a real seeded trim through the real forms, confirmed both via the real public API, confirmed the real public car page renders both — including the battery's real locale-converted range (555 km stored → "345 miles" shown, exactly matching the existing `formatBatteryRange()` logic, not a hardcoded raw value). 6 new `apps/api` integration tests + 1 extended Playwright E2E assertion (continuing the same create-generation-then-trim flow rather than a new test, since it operates on the trim that flow just created). **Real gap found and fixed 2026-09-07 (correct a Generation/Trim/Engine/Battery)**: the same class of gap just closed for Brand/CarModel — all four got a create path (the last three passes) but never a correct one. Fixed with `PATCH /v1/generations/:id`, `PATCH /v1/trims/:id`, `PATCH /v1/engines/:id`, `PATCH /v1/batteries/:id` (all `UPDATE_CAR`-gated, all addressed by real id since none has a public SEO-facing URL, all writing real `AuditLog` rows). `/admin/cars`'s previously read-only generation/trim/engine/battery display was replaced with real inline correction forms at every level. Verified live end-to-end: corrected the real seeded G20 generation's name/start year, the real seeded 330i trim's name, and the real seeded B48 engine's name/power through the real forms (plus a throwaway Battery fixture, since none exists in seed data), confirmed all four via the real public API and the real admin page, then restored the three real seeded values afterward. Extended the existing create-generation-then-trim-then-engine E2E test to also correct each — while doing so, found and fixed 5 of that same test's own earlier assertions that had been checking for plain rendered text (`getByText`), which broke the instant the read-only display became an editable input in this same pass; all switched to asserting the real input's value instead. 15 new `apps/api` integration tests + the corrected/extended E2E test. **Real gap found and fixed 2026-09-07 (aria-label scoping, round 2)**: the earlier same-day fix that scoped `/admin/cars`'s top-level "Add fact"/"Add generation" labels by full car-model identity never got propagated down the rest of the real tree — every label below that (per-generation, per-trim, per-engine, both the "Add" and newly-added "Correct" forms) was only ever scoped by its own immediate name (e.g. `` `Correct trim name for ${trim.name}` ``), not the full path. Unlike the earlier round, this one is a real, not just theoretical, collision risk: trim names like "Sport"/"Base"/"Premium" and generation codes are routinely reused across different real car models. Fixed by computing one fully-qualified label per level (`carLabel` → `genLabel` → `trimLabel` → per-engine), each including everything above it, so no two form controls on the page can ever share an accessible name regardless of naming reuse anywhere in the real catalog. Also fixed the one true duplicate this page always had: the brand-correction form is rendered once per car model (not once per brand), so two models sharing a brand render two forms correcting the same row — scoped its label to include which model it's shown via, rather than restructuring the page to group by brand. Updated 5 of the previous pass's own E2E assertions whose expected label strings changed as a result — including tracing through how each label recomputes fresh on every page load, so a generation rename mid-test changes its trim's and engine's own labels on the next render too, not just the generation's. **Real gap found and fixed 2026-09-07 (validation consistency)**: the numeric fields on the three schemas above (`startYear`/`endYear`, `powerKw`/`powerHp`, `capacityKwh`/`rangeKm`/`rangeMiles`) accepted any number, including negative — caught by re-checking them against this same file's own established convention (`createSourceSchema`'s `trustScore: min(0).max(100)`, `crawlInterval: min(60)`). A negative year, power, capacity, or range can never be real data, so all six now require `.positive()`. 3 new integration tests confirming each rejects a negative value with `400`. **Real gap found and fixed 2026-09-07 (aria-label scoping on `/admin/cars`)**: the "Add fact" and "Add generation" forms' `aria-label`s (`"New fact attribute"`, `"New generation slug"`, etc.) were never scoped per car model — unlike every other per-entity form on this page (per-trim forms scope by `gen.name`, per-generation forms by `trim.name`, and `/admin/redirects`'s own per-row forms scope by `redirect.fromPath`) and unlike this project's own established convention. Harmless while only one real `CarModel` existed, but a real accessibility defect (two form controls with an identical accessible name) the moment a second one does — which this session's own Brand/CarModel creation feature (above) now makes possible. Fixed by scoping every field to `` `${carModel.brand.name} ${carModel.name}` ``. Updated the 2 existing E2E tests that referenced the old unscoped labels to use the fully-scoped names explicitly (they were passing only via Playwright's substring-matching default, which would have broken the moment two real car models coexisted during a test run). **Real gap found and fixed 2026-09-07 (market picker for facts)**: 4 real `Market` rows (US/GB/ES/MX) have existed since `seed.ts`'s original scaffold, and `POST /v1/cars/:brandSlug/:modelSlug/facts` has accepted an optional `marketId` since that endpoint was built — but nothing could ever read the market list back, so the "Add fact" form (whose own placeholder example is literally "price") had no way to offer a market picker, silently making every editor-added Fact global. Fixed with a new public `GET /v1/markets` (reference data, same "no auth needed" precedent as `/v1/cars`/`/v1/topics`) and a real `<select>` on the "Add fact" form wired through the existing proxy route. Verified live end-to-end via curl: submitted the real form with a real market selected, confirmed the resulting `Fact` row's `marketId` and its `(GB)` display on the page. 2 new `apps/api` integration tests + 1 extended Playwright E2E assertion (not a new test, matching this session's "extend, don't duplicate" pattern). **Real, severe gap found and fixed 2026-09-08**: unlike every other parent reference in this file, `marketId` on `POST /v1/cars/:brandSlug/:modelSlug/facts` was never checked to reference a real `Market` row before reaching `prisma.fact.create()` — verified live against a real running dev server (not assumed) that a bogus `marketId` threw a real Prisma P2003 foreign-key violation that, with no async-error-catching shim under Express 4, was an uncaught rejection that **crashed the entire API process** (confirmed dead: `/health` itself stopped responding for every other in-flight request afterward, not just the bad one) — see the "Security response headers" row's `express-async-errors` fix for the systemic half of this fix (no future unhandled async rejection anywhere in `apps/api` can crash the process again); this row's half is the specific existence check giving a bad `marketId` a clean `404 market_not_found` instead of even reaching the generic 500 the shim alone would produce. 1 new `apps/api` integration test (asserts the clean 404 AND that `/health` is still alive afterward — the actual point of the test). `originalCurrency`/`originalValue` (spec §92, "never discard the original currency") remain a separate, deliberately-deferred gap — no currency-conversion UI/logic exists to populate them meaningfully yet. **Real gap found and fixed 2026-09-07 (redirect management)**: every real `Redirect` row could only ever come from `seed.ts` — no editor could add a new redirect (e.g. after renaming a car's slug) or fix a wrong `toPath` without a manual DB write, even though `GET /v1/redirects/lookup` + `apps/web`'s `proxy.ts` have driven real 301s off this table since early in this project. Fixed with `POST /v1/redirects` (409 on a duplicate `fromPath`, 400 if either path is missing its leading `/`) and `PATCH /v1/redirects/:id`, both gated on `MANAGE_SYSTEM_SETTINGS` (no dedicated `PermissionKey` exists for redirects specifically, unlike the other three gaps above — reused the permission already gating `/v1/alerts` rather than adding a new enum value + migration for one small CMS feature) and writing real `AuditLog` rows. Verified live end-to-end: added a real redirect through the real form, confirmed the real public (unauthenticated) `/v1/redirects/lookup` endpoint serves it immediately, corrected its target through the real per-row form. **Real test-infrastructure gap found and fixed in the same tick**: `apps/api`'s integration-test file had accumulated five separate per-describe-block admin logins over the course of the day (each earlier fix's own `beforeAll` pattern, applied independently) and silently exceeded the real login rate limit again — caught by actually running the suite (four new tests failed with 401s traced to an upstream 429). Fixed for good, not patched again: consolidated to one file-level shared `adminAgent`, used by every block except the handful of tests whose actual point is exercising the login mechanism itself. Total real logins in the file dropped from 11 to 5. **Real gap found and fixed 2026-09-07 (analytics/search observability)**: `apps/web`'s `proxy.ts` has written a real `AnalyticsEvent` on every pageview, and `GET /v1/search` has written a real `SearchQuery` row (with its actual result count) on every search, since each feature shipped — 1500+ and 300+ real rows in this dev DB. Both were exactly as write-only as `AuditLog` used to be. Fixed with `GET /v1/analytics/pageviews` (grouped real per-path counts, most-viewed first) and `GET /v1/search-queries` (recent real queries newest-first), both `MANAGE_SYSTEM_SETTINGS`-gated read-only endpoints (no mutation, so no `AuditLog` writes needed), plus `/admin/analytics` visually flagging zero-result searches — the real actionable editorial signal (a reader searching for something the site doesn't have yet). Verified live against real accumulated data, not a fixture: the real top page (`/admin`, 354 real hits) and a real zero-result query both render correctly. **Real gap found and fixed 2026-09-07 (source management UI)**: `PATCH /v1/sources/:id` (`MANAGE_SOURCES`-gated) has existed since early in this project — predating today's session entirely — and is fully tested, but no UI had ever called it; every real use of it before today was via curl or a test client. `/admin`'s Sources dashboard gained the missing controls: an Activate/Deactivate button and an inline trust-score/crawl-interval correction form per source, both posting through a new `/api/admin/sources/[id]` proxy route to the existing endpoint. Also fixed `GET /v1/sources`, which was missing `crawlInterval` and `robotsStatus` from its response entirely (needed to pre-fill the new form and show the robots.txt status added earlier the same day). Verified live end-to-end: deactivated a real fixture source and corrected its trust score through the real button/form, confirmed both in the DB. **Real gap found and fixed 2026-09-07 (add a new source)**: `PATCH /v1/sources/:id` has let an editor correct an *existing* source's trust score/active/crawl-interval since early in this project, but nothing ever let one add a brand-new source — every one of the 3 real sources this project has ever ingested came from `seed.ts`, even though the entire ingestion pipeline is built around `Source` rows. Same "manual DB write required" gap already fixed for Facts/Redirects/Users, just not yet applied to the one entity this pipeline actually revolves around. Fixed with `POST /v1/sources` (`MANAGE_SOURCES`-gated, 409 on a duplicate `url`, writes a real `AuditLog` row) and a new "Add source" form on `/admin` (name/URL/feed URL/type/tier, matching real `SourceType`/`SourceTier` enum values, real schema defaults — `UNVERIFIED` tier, 30 trust, active, 900s crawl interval — apply exactly like `seed.ts`'s own sources) through a new `/api/admin/sources` proxy route, reusing the same query-param error-surfacing pattern already established on `/admin/redirects`. Verified live end-to-end: logged in through the real proxy, added a real source through the real form, confirmed it via the real public `GET /v1/sources`, confirmed the real duplicate-URL error redirect. 4 new `apps/api` integration tests + 2 new Playwright E2E tests (create flow + duplicate-URL error flow). **Real gap found and fixed 2026-09-07 (logout)**: `POST /v1/auth/logout` has existed the whole project and correctly clears the real session cookie, but nothing in `apps/web` ever called it — there was no way to log out through the UI at all, only by manually clearing cookies. Fixed with a "Log out" control on every admin page (a plain `<form>`, zero client JS) through a new `/api/admin/logout` proxy route. **Real bug caught by the E2E suite itself while adding this**: the first version nested the new `<form>` inside the existing `<p className="story-meta">` line — invalid HTML (`<form>` isn't phrasing content, so browsers implicitly close the `<p>` first), causing a real SSR/hydration mismatch that broke two unrelated tests on other pages in ways that looked like regressions in those features. Fixed by using `<div>` instead of `<p>` for that line everywhere; confirmed both previously-failing tests pass again with no other changes needed — they were never actually broken, the shared line's invalid nesting was. Verified live end-to-end: clicking "Log out" ends the real session (confirmed a follow-up visit to `/admin` redirects to `/admin/login` again, not just that the button navigated somewhere). **Real gap found and fixed 2026-09-07 (source trust-score history)**: `docs/editorial-system.md` documents `SourceScoreEvent` (append-only) as how a `Source.trustScore` "moves" over time — it sat unused since the schema scaffold with zero write sites. The fully algorithmic version described there (a source earning/losing score from real corroboration/contradiction) genuinely needs a research/fact-checking AI stage that doesn't exist yet — correctly not invented here. But a human editor correcting `trustScore` through the already-built `PATCH /v1/sources/:id` is a real, already-happening way the score moves today, and the table's generic `reason` string accommodates that without contradicting its documented future use. Fixed: every real trustScore change through that endpoint now writes a real `SourceScoreEvent` (delta/newScore/a human-readable reason); a new public `GET /v1/sources/:id/score-events` (matching `/v1/sources`'s own public-read precedent) reads it back; `/admin`'s Sources dashboard shows each source's real history in a collapsed `<details>` (matching `/admin/audit-log`'s before/after pattern), correctly showing "No trust-score changes recorded yet." for the 2 of 3 real seeded sources that have never been corrected. Verified live end-to-end: corrected a real source's trust score through the real form, confirmed the real event in the DB, the real API response, and the real rendered page — then cleaned up the manually-created fixture rows from the shared dev DB afterward. 4 new `apps/api` integration tests (including one confirming NO event is written when trustScore is unchanged) + 1 extended Playwright E2E assertion. **Real gap found and fixed 2026-09-07 (Stories pagination)**: `GET /v1/stories` accepted a `limit` but never an `offset`, and `/admin/stories` always called it with a fixed `limit: 50` — with 150+ real Stories in this dev DB and growing every real ingest cycle, an editor could never see any Story older than the most recent page through the admin UI at all, no matter how many real stories actually existed. Fixed with a real `offset` param (fetches `limit + 1` rows to detect a real next page without a separate `COUNT` query) and real "← Newer"/"Older →" links on `/admin/stories`. **Real accessibility bug caught by this project's own a11y E2E suite while building this, not shipped**: the new pagination `<nav>` had no `aria-label`, making it indistinguishable from the page's own site-nav landmark — `axe-core`'s real `landmark-unique` rule flagged it live; fixed with `aria-label="Stories pagination"`. Verified live via curl across multiple real pages (confirmed the exact real story count where pagination terminates) and one new Playwright E2E test (skips gracefully if a future clean DB ever has ≤50 real stories, since the test's whole point is exercising real pagination over real accumulated data, not a fixture). 3 new `apps/api` integration tests. **Real gap found and fixed 2026-09-07 (Alerts/Audit-log pagination, same day, later pass)**: the identical structural gap existed on `GET /v1/alerts` and `GET /v1/audit-log` (both hardcoded `take: 100`, no `offset`) — checked live before writing the fix and found this dev DB already had **1,562 real AuditLog rows**, meaning `/admin/audit-log` had been silently hiding 1,400+ real historical entries the entire time, a more severe instance of the same bug than Stories. `/admin/alerts` had only 1 real row so the same-day fix there was lower-urgency but structurally identical. Fixed both with the same `offset`/`limit+1`/`hasMore` pattern, and gave both new pagination `<nav>`s a real `aria-label` from the start this time (learned from the Stories a11y miss above, verified live via the a11y E2E suite rather than assumed). Verified live via curl (audit-log page 1 vs. page 2 across real 200+ rows) and a new Playwright E2E test for `/admin/audit-log` (graceful skip if ≤100 real rows; `/admin/alerts` only got unit-test coverage since it has just 1 real row, not enough for a meaningful E2E pagination walk). 4 new `apps/api` integration tests. **Real gap found and fixed 2026-09-07 (Analytics search-query pagination, same day, third pass)**: checked every remaining admin list endpoint for the same shape after finding it twice already — `GET /v1/search-queries` (the "Recent searches" feed on `/admin/analytics`) had the identical hardcoded `take: 50`, no `offset`, and this dev DB already had **722 real SearchQuery rows**, so 672 real searches (including any older zero-result ones — the whole editorial point of that view) were permanently unreachable. Checked every other unbounded/limited admin list at the same time (`/v1/sources`, `/v1/users`, `/v1/redirects`, `/v1/brands`, `/v1/cars`, `/v1/analytics/pageviews`) — all either have too few real rows to matter yet (sources/users/redirects/brands/cars are single-digit-to-low-tens counts, curated data, not auto-generated per-request like AuditLog/SearchQuery) or are genuine top-N rankings rather than chronological history (`pageviews` groups by path and ranks the top 20, not a feed that should ever show everything). Fixed `/v1/search-queries` with the same `offset`/`take:limit+1`/`hasMore` pattern and gave `/admin/analytics`'s new pagination `<nav>` a real `aria-label` from the start. Verified live via curl with a real admin session cookie across three real pages (offset 0, 50, 700 — confirmed "showing 1–50" → "51–100" → "701–727", `hasMore` correctly false only on the last). 2 new `apps/api` integration tests, 1 new Playwright E2E test (graceful skip if a future clean DB has ≤50 real rows). **Real gap found and fixed 2026-09-07 (Redirects cycle guard, same day, fourth pass)**: neither `POST` nor `PATCH /v1/redirects` rejected a redirect whose `toPath` creates a real infinite loop — a self-referential `fromPath === toPath`, or a two-hop cycle (creating A→B when B→A already exists) — even though `apps/web/src/proxy.ts` resolves exactly one hop per request via a genuine browser 301/302, so either case sends a real browser into `ERR_TOO_MANY_REDIRECTS` the moment anyone visits either real path. Fixed with a deliberately bounded check (direct self-reference + one reverse-hop lookup, not a full N-path graph walk — the same "cheap check that runs first" posture as the rate limiters) on both create and correct. Verified live via curl with a real admin session: a self-loop attempt and a two-hop cycle attempt both real 409s, a normal redirect still real 201s. 3 new `apps/api` integration tests (self-loop rejected, two-hop cycle rejected on create, same two cases rejected on correct). **Real, wide-reaching gap found and fixed 2026-09-07 (same day, one more pass)**: while re-verifying the redirect-cycle guard above through the actual web UI (not just apps/api's own integration tests), found that `apps/web/src/app/api/admin/redirects/[id]/route.ts` discarded the real API response entirely and always redirected as if the correction succeeded — so a real admin correcting a redirect into a cycle saw **no error at all**, just a silent no-op with the old value still showing, even though apps/api's own guard was correctly rejecting it with a real 409. Checked every other "correction"-style proxy route handler in `apps/web` for the same shape and found it was systemic: **9 more** (`batteries/[id]`, `brands/[id]`, `engines/[id]`, `generations/[id]`, `trims/[id]`, `cars/[brand]/[model]`, `facts/[id]`, `sources/[id]`, `alerts/[id]/resolve`) all had the identical bug, plus **one create-flow outlier** (`cars/[brand]/[model]/facts`, fact creation) that broke an otherwise-consistent pattern where every other real "create" handler already checked the response and surfaced `?error=`. Fixed all 11 the same way: check `apiRes.ok`, redirect with the real error code on failure instead of silently continuing. Updated `ERROR_MESSAGES` maps on `/admin/cars` and `/admin` (added `not_found`) and `/admin/redirects` (added `not_found` + `redirect_cycle`), and added the error-display box to `/admin/alerts` from scratch — that page had never had one at all, unlike every sibling admin page. Verified live end to end, not just unit-level: a new Playwright E2E test corrects a real redirect into a self-referential cycle through the actual browser UI and confirms a real, visible error message appears (`?error=redirect_cycle`) and the underlying DB row is genuinely untouched, not just that the page happened to show an error while the write silently went through anyway. 1 new Playwright E2E test; every existing "adding and correcting" E2E test (which all use valid inputs) confirmed still passing unchanged. **Small follow-on fix, same pass**: cross-referenced every distinct error code `apps/api` can ever return against every admin page's `ERROR_MESSAGES` map and found `/admin/users` was missing `not_found` (a real, if rare, response from `PATCH /v1/users/:id/status` when the user being suspended/activated was already deleted) — added it, completing the same audit across all five admin pages with mutation forms. **Real data-quality gap found and fixed 2026-09-07 (same day, one more pass)**: neither `POST`/`PATCH` for Generation ever checked `endYear` against `startYear` — the real public car page renders this pair literally (`{startYear}–{endYear}`), so a data-entry mistake (the two fields swapped) would publish a nonsensical range like "2024–1990" with nothing anywhere catching it. Fixed with a schema-level `.refine()` on create (both fields checked together) and a separate handler-level check on correct (merges the submitted field(s) with the row's own existing, unchanged value before comparing, since a PATCH can legitimately touch only one of the two) — a correction that only sends a new `endYear` is checked against the real existing `startYear` already in the database, not just what's newly submitted. 2 new `apps/api` integration tests. **Real gap found and fixed 2026-09-07 (same day, one more pass, slug format)**: none of the four real `slug` fields (Brand, CarModel, Generation, Trim) validated case or character format at all — every real seeded slug (`bmw`, `3-series`, ...) is lowercase-kebab-case only by convention, never enforced, and the real "Add brand"/"Add car model" forms only show a lowercase example in their placeholder with nothing stopping an admin from typing "BMW" instead. Since `Brand.slug` is a case-SENSITIVE `@unique` Postgres column feeding a real, indexable URL (`/cars/:brand/:model`), that would silently create a second, visually-near-identical brand rather than conflicting with the real existing one — a genuine duplicate-content/SEO risk, not just cosmetic, and the same shape of data-entry mistake as the Generation year-swap and country-code-casing fixes earlier the same day. Fixed with a shared `slugSchema` (lowercase letters/digits/hyphens only, no leading/trailing/double hyphens) applied to all four create endpoints. Confirmed every real existing fixture slug across the entire test suite already conforms (all 131 `apps/api` tests still pass unchanged) — this was purely an unenforced convention, not a hidden dependency on mixed-case slugs anywhere. 4 new `apps/api` integration tests (one per endpoint). **Real robustness gap found and fixed 2026-09-08, same investigation as `apps/web`'s homepage fix**: swept every remaining `Promise.all()` in `apps/web` for the same "does this combine independent work that shouldn't fail all-or-nothing" shape and found 4 more real instances, all fixed the same way (`Promise.allSettled` + server-side logging of any rejection). Two are per-item-over-a-real-list, the same shape as the homepage (`/admin/cars`'s `getCarModel()` call per car model — filtered out on failure, same tolerance the page already had for a genuinely absent one, plus a stale `if (!result) return null` dead-code check removed once the array could no longer contain nulls; `/admin`'s own dashboard `getSourceScoreEvents()` call per source — the FIRST page every admin session lands on, unlike the other three). The other two (`/admin/analytics`'s pageviews+queries, `/admin/users`'s users+roles) are a different, narrower shape: both already had a `.ok ? ... : fallback` per call proving the author already intended independent graceful degradation — the one gap was a genuine network-level rejection (a real, plausible `AbortSignal.timeout` firing on either call) bypassing that intent by rejecting the whole `Promise.all` before either response's `.ok` could even be checked. `/admin/cars`'s own logic verified live with a real throwaway script (one rejecting, one absent, two real — confirms partial success, index/count correctness, no throw) before trusting it; full regression re-run clean after all 4 fixes (269/269 unit tests, real `next build`, 66/66 E2E). `sitemap.ts`'s own 2-item `Promise.all` checked and deliberately left as-is: both calls are cheap, always-available slug-list enumerations, not N independent per-item detail fetches — a different, lower-risk shape than any of the 4 fixed here. |
| Analytics pageviews (`AnalyticsEvent` entity, spec §49) | **Done, tested live + locked in by E2E** — real end-to-end: `apps/api`'s `POST /v1/analytics/events` + `apps/web`'s `proxy.ts` firing a non-blocking pageview event (`event.waitUntil`, never adds response latency, silently swallows failures) on every genuine navigation. Skips Next.js's own link-prefetch requests so they don't inflate counts. 2 Playwright tests assert directly against the real DB (a server-to-server call, invisible to browser-level network interception): a real navigation increments the real row count for that path; the redirect path (`/cars/bmw/3series`) does NOT double-count since it returns before reaching the pageview call. |
| Admin pages excluded from indexing | **Done, tested live** — real gap fixed: `/admin/*` had no `noindex` and `robots.txt` didn't disallow it (an earlier comment had wrongly assumed only `apps/api` needed excluding, missing that `/admin` is served by `apps/web` itself). Fixed with `robots.txt`'s `Disallow: /admin/` + a shared `admin/layout.tsx` emitting `noindex, nofollow` on every admin page — defense in depth, verified both are live while the homepage is unaffected. |
| Redirects (`Redirect` entity, spec §29-31/§38) | **Done, tested live** — real end-to-end: `apps/api`'s `GET /v1/redirects/lookup` + `apps/web`'s `proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts`; migrated after a real build warning, verified the redirect still works post-migration) checks every non-static request against it. Verified with a real seeded redirect (`/cars/bmw/3series` → `/cars/bmw/3-series`, 301) — the old path 301s, the new path and homepage are unaffected. Known simplification, documented in the proxy file: one lookup per request (no in-memory cache), fine at MVP redirect volume, revisit if the table grows large. **Real reliability gap found and fixed 2026-09-07 (fetch timeouts, apps/web)**: extending the same-day `apps/worker` fetch-timeout fix to `apps/web` — none of its 7 real calls into `apps/api` (the redirect-lookup + pageview-recording fetches in `proxy.ts`, every public-page fetch in `lib/api.ts`, and both session-check/adminFetch calls in `lib/admin-auth.ts`) had a timeout. `proxy.ts`'s case is the most severe: it runs on **every real page request site-wide**, and its own comment states a redirect-lookup failure "must never break normal navigation" — but a genuine hang (apps/api's DB pool exhausted, a slow query, real operational trouble, not contrived) isn't a caught failure, so it would have silently defeated that exact fallback and hung the entire site. Also closes a real gap in the same day's own `error.tsx` fix (above): that boundary only catches a *thrown* error, never a pending promise that never resolves, so without this fix a hung apps/api would bypass the new error page entirely rather than triggering it. Fixed with `AbortSignal.timeout(10_000)` on all 7 call sites. Verified live: started both real dev servers and exercised every one of the 7 fixed call sites (homepage, car page, topic page, the real seeded redirect, search, an unauthenticated `/admin` redirect, and an authenticated `/admin/alerts` read via `adminFetch`) — all still respond correctly and quickly with the signal present. `apps/web` has no unit-test tier of its own (verified via typecheck/build/E2E only, per its own `package.json`) — full `npm run build --workspace=apps/web` clean, full E2E suite re-run clean (53/53, unchanged). |
| System alerts (`SystemAlert` entity, spec §55-56) | **Done, tested live with real induced failures (two kinds), and now resolvable** — `apps/worker`'s `ingestSource()` creates a real `WARNING` alert on a crawler failure (deduped: a source stuck failing with the same error doesn't spam a new alert every cycle). Verified by actually breaking a source's `feedUrl`, running a real ingest, confirming exactly one alert row + it renders on `/admin/alerts`; running again with the same failure created zero new rows; fixing the URL cleared `Source.lastError`. **Second kind added 2026-09-07**, once the BullMQ migration made it possible: `apps/worker/src/failure-alerts.ts`'s `handleJobFailure()` creates a real `CRITICAL` alert when a job exhausts all its retries. Verified live: queued a real job for a nonexistent source, watched all 3 real retries genuinely fail with a real Prisma `NotFoundError`, confirmed exactly one `CRITICAL` row with correct metadata, confirmed it renders on the real logged-in `/admin/alerts` page. 3 unit tests (exhausted-retries creates an alert; mid-retry does not; an undefined job doesn't throw). `GET /v1/alerts` is `MANAGE_SYSTEM_SETTINGS`-gated (first admin page needing more than the public read endpoints — added `adminFetch()` cookie-forwarding helper to `apps/web`'s admin lib for it). **Real gap found and fixed 2026-09-07**: `resolved`/`resolvedAt` existed in the schema and were already readable (`GET /v1/alerts?resolved=`) and displayed ("resolved"/"unresolved" per row on `/admin/alerts`) — but nothing anywhere could ever actually set `resolved`, so alerts accumulated forever with no way to mark them handled. Fixed with a new `PATCH /v1/alerts/:id/resolve` (same `requirePermission`+before/after `AuditLog` pattern as `PATCH /v1/sources/:id`), a same-origin proxy Route Handler (`apps/web/src/app/api/admin/alerts/[id]/resolve/route.ts`, extending `adminFetch()` to forward non-GET methods), and a zero-client-JS `<form method="post">` "Resolve" button per unresolved row. Verified end-to-end live: a real seeded alert, resolved through the real UI, confirmed gone from "unresolved" state in the DB, the UI, and a real `AuditLog` row. 4 new `apps/api` integration tests (401 unauthenticated, 404 for a nonexistent alert, real resolve + audit row) + 1 new Playwright E2E test driving the actual button click through to a real DB assertion. **Real gap found and fixed 2026-09-08**: `apps/worker/src/failure-alerts.ts`'s `handleJobFailure()` (the CRITICAL-alert path for a job that exhausts all retries) had no deduplication at all, unlike `ingestSource()`'s own sibling WARNING-alert path (which already compares against `Source.lastError` and skips a duplicate for the same recurring failure) — a permanently-broken source would raise a brand-new CRITICAL alert on every single scheduled run (as often as every real `crawlInterval`, 900s minimum), flooding `/admin/alerts` with duplicates of the identical failure forever. Fixed by querying the most recent matching `source: "worker"`/`severity: "CRITICAL"`/exact-`message` alert and skipping creation if one already exists — mirrors `ingestSource()`'s own "same message = same known failure" semantic via a query instead of a persisted field, since this function has no `Source` row of its own to update. Deliberately does not consider `resolved` status, matching the pattern it mirrors — a resolved alert for the identical message would otherwise let the same permanent failure spam again right after an admin acknowledges it. 1 new `apps/worker` test (same recurring failure across two separate job runs creates exactly one alert; a genuinely different failure for the same source still alerts). |
| Global search (spec §32, `SearchQuery` entity) | **Done, tested live — and a real bug caught and fixed while verifying**: initially used pg_trgm `similarity()` for keyword search, which scores a short query against a whole long title very low (~0.06-0.09 for "Tesla" against real ingested titles that clearly contain it) since it compares the whole strings, not substrings — switched to `word_similarity()`, verified against real data that it now finds real "Tesla" stories and the real "BMW 3 Series" car. Plain `<form method="get">` search page, zero client JS. Logs every real query to `SearchQuery` with its actual result count. Added a real `SearchAction` to the homepage's `WebSite` JSON-LD now that search genuinely works (wasn't added before, to avoid claiming a capability that didn't exist yet). |
| Knowledge Graph edges (`EntityRelation`, spec §25) | **Real schema bug found and fixed, then wired end-to-end + now auto-populated** — the first-ever real INSERT attempt failed with a Prisma FK error: three simultaneous `@relation` directives (to Story/Person/Company, all on the same `fromId` column) each created a REAL enforced foreign key, so `fromId` had to be a valid id in all three tables at once — impossible, which is exactly why this table sat unused since the initial scaffold. Fixed by removing those relation attributes (the table is validated in application code by design, never by real FKs, per its own original comment that the schema code just didn't match). The car page's "Related stories" section reads from real edges. Real automated entity extraction added 2026-09-07: `packages/editorial`'s new `extractCarModelMentions()` (a rule-based stand-in for the AI Researcher's entity-linking, same honesty convention as topic-classifier/pg_trgm clustering) runs in `apps/worker`'s `ingestSource()` on every item, matching only a CarModel's own name + real trim names — **deliberately never the Brand name alone**, since e.g. "BMW" appearing in a headline about an unrelated BMW model would create a wrong edge; precision over recall, same tuning philosophy as the clustering threshold. Locked in with tests proving both a real match (a fixture title mentioning "330i") and two real non-matches (the actual "3, 5 And 7 Series" recall headline that needed manual seeding in the first place; a brand-only mention). **Real gap found and fixed 2026-09-07**: `docs/database.md` claimed `fromType`/`toType` are "validated in application code," but no such validation existed anywhere — every write site used raw string literals (`"story"`, `"car_model"`) with nothing to catch a typo, which would silently create a row the read-path query just never matches (exactly the failure mode that made this table sit unused in the first place). Fixed with `packages/types`' new `ENTITY_TYPE` const object — every write/read site (`apps/api`, `apps/worker`, `packages/database/src/seed.ts`) now uses `ENTITY_TYPE.STORY`/`ENTITY_TYPE.CAR_MODEL` instead of bare strings, so a typo'd type fails to compile instead of silently corrupting data. Lives in `packages/types` (not `packages/editorial`) since not every call site already depended on editorial. **Small cleanup, same day, one more pass**: `packages/types`'s own `ClusterVerdict` type (`"SAME_ARTICLE" | "SAME_STORY" | "NEW_STORY"`) had zero real consumers anywhere in the codebase — confirmed by grep before removing, not assumed; the real, already-working story-clustering logic in `apps/worker/src/ingest.ts` never used it, only its own local types. Removed. |
| Topic classification (`Topic`/`Story.primaryTopicId`, spec §33/§66) | **Done, wired end-to-end + homepage sections** — same "unused schema entity, wire it up" pattern as `Redirect`/`EntityRelation`: `packages/editorial`'s rule-based `classifyTopic()` (first-match keyword rules, safety-recalls > electric-vehicles > market-business, more-specific-first ordering so an EV recall classifies as safety not EV) runs once at Story creation in `apps/worker`'s `ingestSource()`; `GET /v1/topics/:slug` + `apps/web`'s `/topics/[slug]` page expose the real per-topic feed; the homepage now shows a real section per topic below "Latest" (only topics with ≥1 real story render — no fake empty sections). A new `apps/worker/src/backfill-topics.ts` (`npm run backfill:topics`, idempotent) retroactively classifies Stories ingested before this wiring existed — found and fixed 2026-09-07 after noticing 138 of 139 real Stories had never been classified (the wiring only ever ran on new stories going forward); real run: 66 electric-vehicles, 10 safety-recalls, 4 market-business, 59 genuinely unmatched. A stand-in for spec's AI Classifier pipeline stage, same as pg_trgm clustering stands in for the AI Clusterer — not dressed up as AI-powered. **Real classification-quality gap found and fixed 2026-09-08**: checked every real ingested Story title in the dev DB against `classifyTopic()`'s rules and found several genuinely safety/fatality headlines (a driver killed by a Tesla on Autopilot, a fatal-crash lawsuit, a second driver who died with Autopilot engaged) were falling through to "electric-vehicles" purely because they mention "tesla"/"electric" and matched none of the safety keywords. Added `killed`/`died`/`fatal` to the safety-recalls rule — each verified against *every* real story title in this dev DB with zero false positives before adding, not assumed safe. Deliberately did NOT add the broader `kill` or `death`: `kill` alone would have misclassified a real, unrelated story ("Tesla killing Solar Roof", a product-discontinuation piece) as safety-recalls, and `death` would have misclassified another real story using "death spiral" as a business metaphor — both confirmed live against real data before being ruled out. Doesn't retroactively reclassify the already-misclassified rows currently in the dev DB (`backfill-topics.ts` only ever touches still-`null` classifications, by deliberate design, never overwrites an existing one) — the fix applies to every story classified from here forward. 2 new `packages/editorial` tests (the 3 real fatal headlines now correctly classify as safety-recalls; the 2 real near-miss headlines stay correctly unaffected). **Same pass, second real gap**: checked the 67 real currently-unclassified Story titles too and found genuine layoff/job-cut headlines ("VW to cut 100k jobs...", "Jaguar Land Rover Opens Voluntary Layoffs...") going entirely unmatched. Added `jobs`/`layoff` to market-business — again verified against every real title first. A tempting third candidate, `cfo` (for corporate-news headlines), was ruled out after finding a real false positive: it's a literal mid-word substring of "Macfox" (an e-bike brand) in an actual real, unrelated story title. 2 more new `packages/editorial` tests (the 2 real layoff headlines now correctly classify; the real "Macfox" headline stays correctly unclassified). |
| Story timeline (`StoryEvent`, spec §9/§52 "story evolution") | **Done, wired end-to-end** — another previously-unused schema entity: `apps/worker`'s `ingestSource()` now writes a real `StoryEvent` at Story creation (`"discovered"`, naming the source) and at each clustering match into an existing Story (`"new coverage"`, naming the source and the real pg_trgm similarity score). Added the project's first DB-integration test for `ingestSource()` itself (`apps/worker/src/__tests__/ingest.test.ts`, 2 tests, real DB + mocked `fetch`, both paths verified including that a second near-duplicate item genuinely clusters and produces the second event) — this had only ever been verified manually/live before. `/v1/stories` and `/v1/topics/:slug` now include each story's 5 most recent events; rendered on `/admin/stories` (an editor's actual timeline view), not the public homepage. Verified live: real seed backfill on the existing BMW recall Story renders correctly through curl and the real logged-in admin page. |
| Article pages | **Stale row, corrected 2026-09-09** — same shape as the AI-provider row above: left over from the original scaffold, never updated. Real and live at `/articles/:locale/:slug` (133 published as of this correction) — see row 19 above for the actual implementation. |
| AI Editorial Agent (autonomous scheduler) | **First real piece shipped 2026-09-09** (user's explicit request): a full autonomous editorial-judgment agent is not built, but its most concrete, immediately actionable gap was — better duplicate-story detection. Root-caused the exact live bug first (two real published articles, "Polestar reveals Formula 2030..." and "Polestar's Formula 2030 Concept Reveals...", both about the identical announcement from InsideEVs/Electrek, manually archived as duplicates 2026-09-08): `ingest.ts`'s `TITLE_SIMILARITY_THRESHOLD` (pg_trgm, pure text overlap) scored the real two source headlines at only 0.28 — structurally incapable of connecting headlines this differently worded about the same event, confirmed against the real historical data, not assumed. `SourceArticle.embedding` (`Float[]`) had existed since the initial schema scaffold for exactly this ("pgvector upgrade path") but was `[]` for every real row — nothing ever called an embeddings API. Added `apps/worker/src/embed.ts`: a real OpenAI `text-embedding-3-small` call (no SDK, same raw-fetch posture as `generate-image.ts`), wired into `ingest.ts` as a fallback `findClusterCandidate()` only reaches when the trigram check already found nothing. **Verified the approach against real data before building the pipeline integration, not assumed**: a real, deliberate embedding call against the actual two historical Polestar headlines scored 0.78 cosine similarity, against ~0.11 for an unrelated headline pair — a wide enough real margin to pick `EMBEDDING_SIMILARITY_THRESHOLD=0.55` with real grounding, not a guess. 10 new unit/integration tests (`embed.test.ts`, plus a new `ingest.test.ts` case proving two genuinely differently-worded titles now cluster via the embedding fallback — this test caught a real authoring mistake of its own: an initial version mocked `cosineSimilarity` itself as always-0, which silently broke the very thing it was meant to prove, caught by the test actually failing rather than assumed correct). Real cost: a fraction of a cent per new item (`text-embedding-3-small` is $0.02/1M tokens; a headline is a handful of tokens). This closes one concrete duplicate-detection gap, not the larger "agent that makes independent editorial judgment calls" ambition the row's name describes — the honest scope is "sharper story clustering," not an autonomous agent. |
| Docker Compose / Dockerfiles / Nginx | **Superseded 2026-09-08 — actually deployed and live, not just written.** Everything below this sentence (through the "no real production host/domain exists yet" line) describes the state before a real VPS deploy happened this session — kept for history, not current. **Real current state**: all 3 services (`api`/`worker`/`web`) run as real Docker containers on a real VPS (Timeweb Cloud), behind a real domain `auto.kite99.com` with a real Let's Encrypt cert (issued live via `certbot certonly --webroot`, verified against the real ACME HTTP-01 challenge path) — not through this repo's own `nginx` compose service (host ports 80/443 were already owned by a sibling project's nginx container on the same VPS), but by adding a real server block to that shared nginx (`/opt/ai-music-deconstructor/nginx/nginx.conf` on the server) proxying to `web` over a shared external `webproxy` Docker network. `api` is never exposed publicly — `apps/web`'s own `API_INTERNAL_URL` reaches it server-to-server on the default compose network, matching the architecture this repo's own `apps/web/src/lib/api.ts` already documented. **Real, non-obvious operational gap found and fixed the same day**: `/repo` inside a running container is a build-time `COPY`, not a live bind mount — editing a file on the VPS host and re-running it via `docker compose exec <service> npx tsx ...` silently executes the STALE code baked into the last image build, not the edit (see the AI Writer stage row above for the full story of how this was caught). Any real source change needs `docker compose build <service> && ... up -d --force-recreate <service>` before it's live — a bare `exec` never picks it up. **Second real gap, same day**: `docker compose up -d --force-recreate` on `web` (or `api`) drops its manual `docker network connect webproxy <container>` attachment (that network isn't declared in this repo's own `docker-compose.yml` — it's the sibling project's network, joined by hand) — every recreate needs that reconnect redone, or nginx can no longer reach the container by name and the public site 502s. **Third real gap, same day (branding placeholders baked into static builds)**: `Dockerfile.web` never copied `.env` into its build stage, so `next build`'s statically-prerendered pages (the 404/not-found boundary, confirmed live — Next.js prerenders it separately even in an otherwise-dynamic app) permanently baked in `layout.tsx`'s own fallback literals (`"PROJECT_NAME"`/`"DOMAIN.COM"`) regardless of the real runtime `.env` — only genuinely dynamic pages (a `cache: "no-store"` fetch, like the homepage) ever showed the real name. Fixed with real Docker build `ARG`s (`PROJECT_NAME`/`PUBLIC_URL`/`PUBLIC_DOMAIN`) threaded through `docker-compose.yml`'s `build.args` into `Dockerfile.web`'s `ENV`, reaching `next build` itself — verified live afterward: the 404 page and every real `/about/*` static page now show "Automotive AI Platform" and `https://auto.kite99.com`, not the placeholders. None of this is retroactively documented in `docs/deployment.md` below yet (that file still describes the pre-deploy plan) — a real follow-up, not done in this pass. **Corrected 2026-09-09**: the live domain changed from `auto.kite99.com` to **`autonewsfeed.com`** (site rebranded to "AutoNewsFeed" the same day — real `PROJECT_NAME`/`PUBLIC_DOMAIN`/`PUBLIC_URL` in the server's `.env`, verified live; `auto.kite99.com` now just 301s). `docs/deployment.md` has since been updated to match. Also found and fixed same day: this repo had zero git commits until now (first commit `45771a5`, pushed to `github.com/qiraqira/automotive-ai-platform`) — but the server's `/opt/automotive-ai-platform` still has no `.git` (deployed via direct file copy, not git), so the new GitHub history and the live production containers are not yet connected; a git-pull-based deploy is the next real gap, not yet closed.

The rest of this row, describing the pre-deploy state, kept for history:

Written, not yet run (no Docker CLI/daemon on this dev machine — see docs/deployment.md). YAML validated (`npx js-yaml`). Build-order bugs found and fixed twice now (2026-09-07, same day both times): first pass, all 3 (`Dockerfile.api/.worker/.web`) only ever built `@automotive/database` + the app itself, missing newer workspace packages (`types`/`config`/`ai`/`editorial`/`seo`); second pass, caught that all 3 built `database` **before** `types` even after that fix — `database` itself gained a dependency on `types` the same day (the `ENTITY_TYPE` fix) and these files were never re-checked against it. This is now a demonstrated pattern (3 separate real breaks across 2 ticks), not a one-off: **any change to a `packages/*` dependency graph needs these 3 files re-verified by hand** — nothing enforces them staying in sync automatically. Verified all 3 chains by simulating each file's exact `RUN` sequence locally (clean `dist/`, rebuild in that literal order) since Docker itself can't run here. **Real gap found and fixed 2026-09-07 (nginx)**: `docker-compose.yml`'s `nginx` service has bind-mounted `../nginx/nginx.conf` since this file's initial scaffold, but `infrastructure/nginx/nginx.conf` itself was never actually written — a real `docker compose up` would have failed to start `nginx` at all (bind-mounting a nonexistent host path). Also found the same file was missing a `certbot` service entirely: `certbot_conf`/`certbot_webroot` volumes existed and `nginx` mounted them, but nothing ever populated them with a real certificate. Fixed both: a real, complete `nginx.conf` (HTTP→HTTPS redirect, ACME-challenge webroot passthrough, TLS termination, and the actual routing split — `/health`/`/ready`/`/v1/*` to `apps/api`, everything else including `apps/web`'s own same-origin `/api/admin/*` proxy routes to `apps/web`, since those are a different `/api` than `apps/api`'s real one) and a real `certbot` compose service (a 12h renewal-check loop via the same webroot `nginx.conf` serves). `docs/deployment.md` gained a new "TLS certificates" section documenting the one real manual step this can't automate — the very first certificate's bootstrap run, which needs a real domain/email and can't be an unattended compose step. Same "written and reasoned through by hand, not run-and-verified" honesty as the three Dockerfiles — Docker still can't run on this dev machine — but no longer a dangling reference to a file that didn't exist. **Real gap closed for good, 2026-09-08**: this row's own "nothing enforces them staying in sync automatically" line was true for a full day after 3 separate real breaks -- fixed by writing the missing enforcement instead of relying on "re-verify by hand" a fourth time. New `infrastructure/docker/verify-build-order.js` reads the REAL dependency graph straight from every `packages/*/package.json` "dependencies" field (never hand-copied) and checks all 3 Dockerfiles' `RUN npm run build --workspace ...` sequences against it: every package the app transitively depends on must be built, and each package must be built strictly after its own `@automotive/*` dependencies. Wired in as `npm run verify:docker-build-order` and a new CI step (right after Typecheck, before Test) in `.github/workflows/ci.yml` -- a static check, needs no Docker daemon, so it can run in CI even though a real `docker build` still can't. Verified the check itself actually catches real drift, not just that it passes today: deliberately reproduced both historical failure modes on scratch copies of the real Dockerfiles (removing one required build step; swapping `types`/`database`'s build order) and confirmed the script fails with the exact specific package name and reason in both cases, then confirmed a clean re-run passes -- before restoring and diffing the real files back to byte-identical with their originals so no test artifact was left behind. **Related documentation gap found and fixed 2026-09-08, same tick**: `docs/deployment.md`'s own "CI/CD" section described a full deploy pipeline (build each app's Docker image, deploy over SSH to a production host) as if it already existed -- checked the real `.github/workflows/ci.yml` directly and it's CI only (install, typecheck, test, build, E2E, now also this row's new build-order check), zero `docker build`/SSH/`compose` steps anywhere -- correct, since no real production host/domain exists yet for a CD step to deploy to. Corrected the doc to state the honest current state, with the originally-intended pipeline shape kept as a clearly-labeled future "when a real production host exists" plan rather than a present-tense claim. |
| CI (`.github/workflows/ci.yml`, spec §6) | **Written and the full pipeline verified locally, step by step** — install→generate→migrate→seed→typecheck→test→build, real Postgres **and Redis** service containers (`redis:7-alpine` added 2026-09-07 for the BullMQ scheduler tests), all 260 tests + a real `next build` production build + the 66-test Playwright E2E suite (incl. accessibility + security headers/CSP) confirmed passing locally in this exact order (run twice to confirm no flakiness). Not yet run on actual GitHub Actions (no remote configured for this repo). Caught a real bug while verifying: a clean `npm ci` does NOT reliably regenerate the Prisma client for this monorepo layout — every package lost its custom enum/type exports until `prisma generate` was added as an explicit CI step (was previously assumed to happen automatically via postinstall; it doesn't, reliably, here). **Re-verified 2026-09-08** after adding `express-async-errors` (see "Security response headers" row): a real clean `npm ci` (not just `npm install`) → `prisma generate` → full typecheck (all 10 workspaces) → all 260 tests → `next build` → all 66 E2E tests, run in this exact CI order against a genuinely wiped-and-reinstalled `node_modules`, confirming the new dependency is correctly locked in `package-lock.json` and doesn't disturb any assumption this row already documents. |

## Quickstart (what's actually runnable today)

```bash
npm install
# point DATABASE_URL at a real Postgres (see docs/database.md for the
# isolated local-dev cluster this was built and tested against) and
# REDIS_URL at a real Redis >=5 (see docs/database.md "Local dev Redis" —
# needed for apps/worker's scheduler tests + the real scheduler itself)
npm run db:migrate
npm run db:seed
npm run ingest:once   # fetches real RSS feeds, dedupes, clusters into Stories
DATABASE_URL=... REDIS_URL=... npm test   # 279 tests — apps/api's + apps/worker's scheduler tests need real services, not mocked
DATABASE_URL=... npm run e2e   # 66 Playwright E2E tests (incl. 20 accessibility + security-header/CSP checks) — spins up its own dev servers on ports 3100/4100
npm run backfill:topics   # classifies existing unclassified Stories (safe to re-run — idempotent)
npm run backfill:authors  # links pre-existing authorless SourceArticles to a real SourceAuthor where the source's *current* feed still carries that item (safe to re-run — idempotent)
npm run backfill:importance  # recomputes importanceScore from each Story's real current distinct-source count (safe to re-run — idempotent)
# Real gap found and fixed 2026-09-07: the two lines above were documented
# here as root-level commands the moment their apps/worker scripts were
# added, but the root package.json's own forwarding alias (the same
# pattern backfill:topics already had) was never added alongside them —
# `npm run backfill:authors` from the repo root genuinely failed with
# "Missing script" until this was caught and fixed the same day.
REDIS_URL=... npm run dev:worker   # the real BullMQ-backed scheduler (spec §39) — see status table below
```
